# CMake integration

`ikfom_vendor::ikfom_vendor` is an exported CMake interface target. It provides
an explicit dependency edge and provenance today, but deliberately exports no
IKFoM headers until a licensed, pinned source import is reviewed.
