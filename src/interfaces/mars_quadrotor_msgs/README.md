# mars_quadrotor_msgs

This package contains only the ROS 2 `PolynomialTrajectory` interface used by
the SUPER-compatible navigation pipeline.  The field layout and flag values
are kept compatible with the upstream SUPER message; simulator and tracking
messages are intentionally not imported into the product workspace.
