# mars_quadrotor_msgs

This package contains the ROS 2 `PositionCommand` PVA contract used between
the integrated planner runtime and PX4 External Mode. Simulator and tracking
messages are intentionally not imported into the product workspace.
