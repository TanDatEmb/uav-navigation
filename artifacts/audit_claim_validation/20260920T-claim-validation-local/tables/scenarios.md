# Controlled scenarios and negative controls

| Scenario | Pre-state / event | Ordered effect | Identity / lease / authority | Evidence and result |
|---|---|---|---|---|
| H1 positive | Active pass-through waypoint; fresh finite measured segment crosses acceptance; command carries matching mission/waypoint/request continuation. | Accepted command immediately triggers updateMission; MissionController sees crossing and increments once. | Matching request; command/state/health leases valid; adapter owns mission controller. | TEST_EXECUTED: actual MissionController/RouteProgress native harness. |
| H1 local counterexample | Crossing segment arrives while continuation absent; next update uses same current position after leaving acceptance; matching witness injected late. | Each update refreshes previous_position_; late witness tests only new sample and cannot reconstruct the old segment. | Abstract continuation input; not produced through runtime candidate generation/admission. | LOCAL_COUNTEREXAMPLE and TEST_EXECUTED. Wrong request identity is negative control. |
| H2 fresh world before receiver expiry | Suspended exact bundle; fresh world recertifies same generation/goal/localization before stale guards expire. | Runtime exact-identity rechecks; restores execution episode and may publish again. | Same runtime identity; adapter still checks its own header/receive/health/state/frame/world guards. | FACT_FROM_SOURCE; no paired handler reproduction. |
| H2 no useful recertification through receiver lease | World remains stale or recertification fails; no command emitted by stale-world callback; adapter accepted command ages past 100 ms outside exact completed-recovery exception. | updateSetpoint invokes safetyStopNavigation: failure and handover latches, command invalidated, controller deactivated, stationary stream/status and Hold request; later navigation command callback is blocked by latch. | Adapter becomes final application authority; actual PX4 mode acceptance unobserved. | PRODUCT_PATH_REACHABLE statically under stated preconditions; intended fail-closed policy. |
| H3 lock barrier | Exposure owns runtime/store locks while another callback needs localization lock. | Lock ordering establishes wait until exposure releases. Existing test barriers store lock and verifies freshness is checked after wait. | Test does not execute ROS publication; source places publish inside lock scope. | Structural relation confirmed; induced test is not latency workload. |
| H4 runtime RetryFromRest | Current request in stopped recovery; first failure sets steady start; later retry is delivered. | Runtime elapsed guard can fail-close after five seconds only in eligible stopped state. PX4 timer, if started, begins at completed command observation. | Runtime and adapter use distinct recovery episode keys. | Source-supported; no same-episode mismatch reproduction. |
| H5 governor | Caller supplies measured PVAJ, four metre supports and physical BACKUP limits. | Existing tests cover a positive concrete witness, weakest support, insufficient current support, physical envelope rejection and budget exhaustion. Caller returns before nominal endpoint solve on failure. | Upstream route/corridor gates and downstream world certificate remain separate. | Targeted PlannerSpeedGovernor tests executed; no independent continuous oracle/product false negative. |
| H6 Hold ordering | scheduleMode(AUTO_LOITER) at pinned px4_ros2_interface_lib revision. | Library sends vehicle command and waits ACK, activates scheduled mode, then waits matching ModeCompleted before callback. Application clears pending on Success or Deactivated; VehicleStatus AUTO_LOITER independently sets confirmed/clears pending. | Deactivated can represent operator/mode transfer; retry could reacquire control. Firmware status/callback ordering absent from client library. | ACK-only interpretation refuted; status ordering unresolved without firmware/runtime evidence. |

These rows are not observed flight sequences. Controlled executions are isolated native/gtest tests in validation/commands.md and raw logs.

