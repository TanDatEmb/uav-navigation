# Verdict: SOFTWARE_QUALIFICATION_WITNESS_CLOSED

The **fresh C0-SW witness contract** is closed, with the scoped qualification distinction preserved. Versioned user-approved `C0_SW_V1` is first-class in metadata/evaluator. The five primary clean-source sessions are consecutive C0-SW eligible/PASS after exact offline reevaluation; all required lifecycle/reference gaps and conflicts are zero and 420,486 writer records are loss-free. Producer-owned identities replace recorder inheritance and timestamp-nearest required joins. Release, CTest, Python, static guards and safety ledger passed at evaluator commit `db43b608`; final-HEAD validation is reported in delivery.

This does **not** mean integrated flight qualification passed. All five primary original runners reported `FAIL` under integrated physical criteria. The independent verification cohort had only 2/5 C0-SW eligible: two `RECEIVE_STALE` component failures and one safety stop without an assessed gate witness remain `NOT_EVALUABLE`. Their required lineage and writer accounting are complete. Those failures are in `FRESH_COHORT.csv`, not removed from the denominator. `FAULT_HANDLING` suite evidence and C0-IFP remain separate; the historical 140/5,957 gaps remain missing.

No safety threshold, planner algorithm, command/adapter behavior, world policy or PX4 Hold policy changed. The behavior-bearing diagnostic source commit is `d62319fd`; primary capture source `5a77f5e0`, final evaluator source `db43b608`, and final evidence/documentation HEAD have distinct provenance. This verdict is at software witness level, not HITL/flight or PX4 consumption proof.
