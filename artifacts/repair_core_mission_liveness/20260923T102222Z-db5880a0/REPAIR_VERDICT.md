# Focused first-cut mission liveness repair

Status: **MISSION_LIVENESS_REPAIRED** for the focused first architecture cut. The causal source mechanism is documented in `ROOT_CAUSE.md`. This is not flight qualification or approval of a second architecture cut.

The repair keeps MissionProgress as the sole Core mission writer and the PX4 adapter as the local safety boundary. The final command publication transaction now checks the execution that owns the sampled bundle, not the desired successor gate. No persistent state or threshold was added. Deterministic package tests, Python contract tests, static authority guard, Release build, safety ledger validator and diff check passed. Three consecutive **matched `tracking=off`** `long_featured` sessions completed 5/5 against baseline 2/2 and failed migration 0/3. Three diagnostic `relaxed` completions are supplementary. The maximum observed adapter tracking-setpoint handoff gap was 28 ms versus the unchanged 100 ms command receive lease; no nominal identity/continuity rejection or unintended lease expiry appeared.

Safety evidence is bounded: pillar `off` accepted `[0,1,2]` including the PASS seam, then correctly failed closed on world revision 510 invalidation with no certified brake; PX4 Hold was observed. A separate 650.146 ms Core pause caused the adapter to detect stale PVA, request Hold and observe AUTO_LOITER. That fault also triggered an unconsumed-cloud gate and does not isolate source-time world staleness. Pillar completion and isolated world-stale SITL remain boundary evidence gaps; neither is silently counted as a nominal PASS. Existing world freshness/lease/adapter paths and numeric gates were not changed by this repair. The user-requested liveness parity and single-writer/safety regression gates are met for the focused cut.
