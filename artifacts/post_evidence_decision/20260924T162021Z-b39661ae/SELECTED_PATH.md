# PATH D scope

This branch records concrete policy decisions and the calculations/evidence needed to make them. It does not select numeric tracking or motion thresholds, modify evaluator acceptance, repair lifecycle lineage, start World Temporal Contract, or create a new SITL cohort. Missing policy remains `NOT_EVALUABLE`; missing lifecycle/reference evidence remains incomplete.

The prior user's answer was that no unified threshold document exists and calculation/analysis is needed. `OPTIONS_ANALYSIS.md` uses the available ten-run distribution to characterize the problem and specifies non-circular calculations for a future policy. It does not claim that this small, partially ineligible cohort approves a limit.

No product, PX4, safety ledger, evaluator or runner file is changed on this branch. The architecture and existing 100 ms command lease, 200 ms adapter state boundary, 500 ms world freshness and 0.750 m terminal anchor gate remain untouched.
