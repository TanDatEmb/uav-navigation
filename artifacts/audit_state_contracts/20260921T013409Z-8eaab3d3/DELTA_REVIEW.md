# Frozen A → TARGET delta review

Frozen A is the source snapshot embedded in AS-IS artifact commit `f2bd3f46f9936d622377ea4761f733f988273b66`, not a clean checkout at its recorded HEAD. Its manifest SHA-256 is `f9481b2f7312556ae91a8bff590cbc412de8d7e3e068fc98d4c85bb34c730413`; 596 manifest paths are compared to TARGET git blobs. Full per-file hashes and package membership are in `baseline/A_to_TARGET_delta.json`.

| Claim / area | Source in A | Source in TARGET | Semantic change / consumer | Test evidence and TARGET verdict |
|---|---|---|---|---|
| “Governor still rejects below the fixed 16-sample floor” | `baseline/source_snapshot/src/planning/navigation_planning_backend/include/planner_core/evidence_speed_governor.hpp`; A includes `kBoundedSpeedSamples=16` descending candidate loop. Its A SHA is recorded per path in delta JSON. | `src/planning/navigation_planning_backend/include/planner_core/evidence_speed_governor.hpp` (`ev-governor-delta`) and caller `planner.cpp` (`ev-planner-delta`). The fixed speed grid is gone; measured initial PVAJ stop is checked, then prospective steady cruise uses a closed-form speed cap with continuous extrema and bounded `nextafter` correction. | `Planner::plan` consumes the failure taxonomy before nominal solve and still runs corridor/world validation. Scalar support proposes speed; it is not a world certificate. The “still 16 samples” premise is stale. | `test-governor`: 6 `PlannerSpeedGovernor` cases plus 2 abort cases pass. H5 old-grid premise `REFUTED` for TARGET; completeness and workload cost remain unresolved. |
| “BACKUP time search can be assumed to hit a deadline / lacks cancellation” | A `backup_braking.hpp` and `planner.cpp` hashes appear in the delta manifest; A used prior duration-seed/search behavior. | TARGET `backup_braking.hpp` adds `kBudgetExhausted`, treats the stop envelope as a seed, bounds steady-state to one attempt and nonsteady to at most 24 trials; `Planner::plan` supplies cancellation/deadline. (`ev-backup-delta`, `ev-planner-delta`) | Checks occur before and between polynomial/extrema/support work; that work is not preempted. Trial count is not WCET. Consumer separates budget exhaustion from physical envelope, no support and synthesis failures. | `test-governor` abort assertions pass. No caller workload latency distribution; do not infer a deadline miss or performance fix. |
| “Desired and active identity are duplicate episode fields” | A `ExecutionEpisodeSnapshot` has desired `goal_epoch/request_id` and `active_generation`, but no active command goal/request identity. `beginGoal(..., retain_command)` directly changed phase/availability/recovery to MAIN while retaining generation; see frozen A path and recorded A hash. | TARGET adds `active_command_goal_epoch` and `active_command_request_id`; eligible retention preserves active IDs, generation, phase, role/suffix and recovery until `commandCommitted` changes active identity (`ev-episode-snapshot`, `ev-episode-goal-commit`). | `NavigationRuntimeNode` planning uses desired identity; active observations/recovery use active identity. This distinguishes intended successor from command actually sampled. It is not evidence of duplicated authority by itself. | Five selected `ExecutionEpisode` tests pass, including `HotRetargetKeepsActiveBackupUntilSuccessorCommits`; FSM pending-goal tests also pass. TARGET semantics support intentional identity separation; full candidate/adapter chain remains unjoined. |
| “Initial hold cannot go directly to BACKUP/EMERGENCY” | A recovery helper/episode code and planner tests differ in the delta. | TARGET `execution_recovery_state.hpp` / episode path admits the initial-hold recovery event; runtime planner maps it to the production path (`ev-episode-goal-commit`, `ev-fsm-test`). | Adds a reachable guarded state transition; A transition reachability must not be copied unchanged. | Selected stopped-recovery/emergency tests pass. No claim that every profile reaches it. |
| “CIRI two-pass is current” | A planner config and safety doc hashes differ. | TARGET `planner.yaml`, current safety contract/index (`ev-safety-current`, `ev-safety-index`) set CIRI current value to `1`; `TB-003` leaves two-pass reference unpromoted. | Do not reuse historical two-pass candidate availability or timing as TARGET behavior/profile. | This audit did not run a CIRI A/B; status is source/profile correction only. |
| World-suspension liveness finding | A runtime node had the same central exact-bundle suspension/resume scheme. | Relevant runtime implementation still cancels work, suspends exact active generation, then requires exact latest-world recertification and commit recheck (`ev-world-suspend`, `ev-world-resume`). | No atomic map-callback→adapter lease transaction was added. Adapter command cache/lease remains independent. | Runtime helper/store/node fixture groups pass, but no joined receiver-deadline/handover run. H2 remains `CONDITIONAL`, not a proven liveness defect. |
| PASS_THROUGH/mission and PX4 Hold semantics | A manifest hashes and prior claim-validation artifact record these paths. | The seven focused package memberships match A/TARGET counts; `MissionController`, `RouteProgress`, `NavigationMode` and PX4 adapter implementation are not among the content-changed paths. Current refs include `ev-mission-update`, `ev-route-crossing`, `ev-adapter-mission`, `ev-adapter-hold-callback`. | Prior measured crossing analysis remains relevant. H6 dependency claim is carried forward from claim-validation artifact: pinned `scheduleMode` completion waits for matching `ModeCompleted`, not merely a command ACK. Dependency/consumer did not change, so H6 was not re-audited. Current adapter still clears Hold pending on Success/Deactivated separately from AUTO_LOITER confirmation. | Adapter progression, mission, command contract, and local handover tests pass selected cases. No PX4 target trace/status observation. |

## Scoped package inventory

A and TARGET have identical tracked-file counts and no additions/removals in the packages reviewed deeply: planning backend 108; planning 16; runtime 45; PX4 external mode 29; mission contract 8; navigation contracts 17; committed execution 11. Fifteen manifest content paths changed and two generated build metadata paths are absent at TARGET. Broad `src/` additions outside those package paths include vendor material and are not treated as product delta.

No B/root-checkout source was substituted. The A source files are not recopied; TARGET evidence is pinned to Git blob plus SHA-256 in `evidence/refs.jsonl`.
