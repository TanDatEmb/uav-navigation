# C0-SW World transaction result

The 430 ms world-fault session stores `world_evidence_required: true` and required event kinds in its immutable `scenario_config.yaml`. The evaluator now merges that per-session scenario overlay into the report runtime/evaluation config. Before this wiring correction, a nonempty base config masked session requirements and the reducer incorrectly returned `required=false`; a regression test now covers that case. This change restores declared evidence requirements and does not alter acceptance semantics.

Session reducer result: `required=true`, status `RESOLVED`, minimum required transactions 3, 573 unique transactions observed. Required World unresolved=0; conflicts=0; references missing=0; reference conflicts=0. Producer cumulative event counter ended at 1080. Evidence observed/submitted/written 1029 events, dropped 0; serialization failures 0; writer failures 0. First observed sequence is 52, with producer counter baseline 51; observed sequence 52 through 1080 is contiguous and matches the producer tail. The 51-event difference is the pre-observer cumulative prefix, not an in-capture drop.

Required event classes `WORLD_COMMAND_SUSPENDED`, `WORLD_PUBLICATION_COMMITTED`, `WORLD_COMMAND_RECERTIFIED`, and `WORLD_COMMAND_RESUMED` were all observed. The suspension/recovery sequence is keyed to producer sequence and exact timeline/world/execution identity, never nearest timestamp. Writer accounting reports 1029 submitted/accepted/written, zero dropped; producer-count records also have zero drops; writer pending at finalization is zero.

The overall C0-SW assessment for this fault session remains `NOT_EVALUABLE` (not qualified): `MOTION_ACCEPTANCE_POLICY_UNAVAILABLE`, `REFERENCE_LINEAGE_MISMATCH`, `TRACKING_COVERAGE_POLICY_UNAVAILABLE`, and a dependent required-dimension status. The World transaction axis is resolved; unrelated qualification policy/lineage defects are not modified here.

A post-fix evaluator recheck of the earlier 420 ms configured mapping-only session also resolves its World requirements: `required=true`, all expected stale/recovery event kinds observed, zero required unresolved/conflicts/reference gaps/drops. This session has a separate captured build-content SHA and is supplemental; it is not pooled with the primary 430 ms run or the same-build nominal cohort. See `EVALUATOR_RECHECK_420.json` and `RAW_SESSION_MANIFEST.csv`.
