# Desired intent cleanup verdict

`DESIRED_INTENT_CLEANUP_COMPLETE`

The behavior-bearing product source was last edited at `c9006768` and clean-built at `473b2121`. `DesiredPlanningIntent` is one typed value under the existing RuntimeNode input lock. Its three independent facts are desired goal, monotonic revision, and one mutually exclusive planning transition enum. The former four fields `active_goal_`, `active_goal_epoch_`, `new_goal_`, and `hot_goal_transition_` are gone. The invalid simultaneous new-plus-hot combination is unrepresentable. RuntimeNode's desired and executing identity reconstruction helpers are gone; desired matches use the value API and active matches use `ExecutionAuthority`.

The compatibility `ExecutionTimelineSnapshot`, `ExecutionEpisodeSnapshot`, `ExecutionTimelineStore`, and `CommittedBundleStore` vocabulary and the old header/test names were removed from product code. `MissionProgress` remains the sole mission-progress writer; `ExecutionAuthority` remains the sole active/staged execution owner; WorldModel and PX4 adapter retain their independent authority. Desired successor N+1 may coexist with active predecessor N. New tests exercise that state, stale result discard, continued predecessor publication, atomic cutover, late old sample rejection, and conditional fail-close tied to the exact active snapshot. No safety threshold, planner algorithm, command lease, world freshness, PASS semantics, or Hold policy changed.

Build and deterministic gates: full Release build 23/23 packages; focused CTest 31/31; Python runtime contracts 390 OK, one skip; mission, execution, and desired-intent static guards PASS; safety ledger validator PASS. The final clean-HEAD repetition is recorded by the delivery response after this evidence-only commit. The retained SITL executable came from the clean authoritative manifest at `473b2121`; later documentation commits leave product source identical.

Matched `long_featured`, seed 0, tracking-off diagnostic SITL: **3/3 consecutive mission COMPLETE** with `[0,1,2,3,4]`. Twelve adapter admission handoffs measure `19.958/20.052/20.169 ms` min/median/max, versus the unified-owner reference `19.894/20.030/20.093 ms`, without nominal stale lease, unexpected Hold request, identity reject, or candidate continuity reject. Nominal 1 has one isolated pre-admission `valid=0` sample rejection; its cause remains boundary debt and it did not cause a lease or mission failure. Terminal STOP was accepted at measured `0.0982 m/s`. The Core-pause fault stopped Core for `350.094 ms` after 1,802 observed PVA commands and produced adapter stale-PVA/Hold; repeated-replan injection produced BACKUP and measured low-speed restart while mission completed.

This is an architecture and focused runtime parity verdict, **not flight qualification**. The runner's versioned qualification result remains FAIL/ineligible even for mission-complete runs. Isolated world-source stale, emergency-path SITL, and PX4 Hold callback ordering remain independent boundary evidence debts; see `REMAINING_DEBT.md`. No further architecture cut is proposed here.
