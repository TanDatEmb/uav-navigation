# Semantic differential result

Comparator provenance: pinned product baseline `7da3e97c`; read-only diagnostic shadow branch `9b16aa9a`, `artifacts/shadow_reducer/20260923T074145Z-7da3e97c/DIFFERENTIAL_RESULTS.md`. The shadow replay consumed earlier normalized instrumentation evidence, not this migration binary. It reported 39 matched PASS acceptance events and ten matched STOP events in O1, 6,398 exact downstream command pairs plus 214 ambiguous/unpaired joins across 14 runs, and four W2–W5 lease-expiry/Hold fault observations without old-session downstream admission after the fence. Ambiguous joins remain `INSUFFICIENT_TRACE`.

| Semantic output | Baseline/shadow evidence | Migration component evidence | Live product differential |
|---|---|---|---|
| PASS acceptance order/request | 39 O1 matches; crossing-first PT2 absent in runtime trace | 13 `MissionProgress` model cases and Core initial/successor integration | Migration `long_featured`: ordered accepted prefixes `[0,1]`, `[0]`, `[0,1]` in three runs; request/goal receipts advanced to the next gate. Baseline: `[0,1,2,3,4]` in two matched runs. Semantic completion parity is **not established**. |
| STOP measured completion | Ten O1 matches in shadow, without raw velocity reconstruction | Measured speed, confirmation and hold test | Migration `pillar` accepted STOP 0 and 1 at measured speeds 0.047 and 0.103 m/s, then PASS 2. It did not complete the mission. |
| Successor goal identity | Legacy PX4 goal publication | Core internal validated-goal integration, request 1→2; adapter no goal publisher | Core GOAL receipts in `long_featured` reached gate 2, but the mission later lost a valid command. No PX4 successor goal publication was observed or required. |
| Command admission | O2 exact/ambiguous joins as above | Core rejects missing/foreign/duplicate receipt; adapter rejects old activation and nonmonotonic request | Migration `open` bag had 654 admission receipts and Core acceptance 0–3 before a later world/candidate failure. No evidence of fabricated acceptance. |
| PX4 Hold outcome | Shadow O4 observations, external protocol unresolved | Product Hold implementation unchanged | Controlled 650 ms Core SIGSTOP after accepted commands caused downstream stale-command detection, Hold request and observed PX4 Hold. This is a focused lease-fence observation, not firmware qualification. |

Intentional difference: crossing-first observation persists until a same-route/localization/gate admitted continuation arrives; the legacy callback-local path could lose it. Classification `INTENTIONAL_ARCHITECTURE_FIX`, not a claimed reproduced flight bug. A near-parallel nonadjacent projection now preserves the ordered branch within the existing route tolerance. `NavigationCommandAdmission` and mode activation binding prevent publication-only or old-session evidence from authorizing acceptance.

The matched live comparison used `map_profile=long_featured`, seed 0, tracking mode `off`, the same external PX4 checkout `deaff86e`, and individually validated Release manifests. Baseline `7da3e97` completed 2/2 runs; migration `ccb37342` completed 0/3 and handed to Hold after the current candidate/command became unavailable or stale. Because the failure mechanisms differ among migration runs, these traces establish an unresolved differential liveness regression **signal**, not a proven single-code-path cause. Treat the product cut as failed until the cause is isolated and corrected. See `SITL_RESULTS.md` for exact sessions and the tracking-configuration limitation.
