# Build and component evidence

- Release build: `PARALLEL_WORKERS=2 MAKE_JOBS=4 python3 tools/runtime/build.py --mode release build`; 23 packages built, authoritative manifest `install/.uav_navigation_build_manifest.json` for product HEAD `ccb373422642f27a8740b32ea4ab6062e433e0be`. Log: `/tmp/uav-mission-cut-full-release-after-harness.log`.
- Relevant package test command: `colcon test --packages-select navigation_contracts navigation_mission navigation_execution navigation_runtime px4_navigation_external_mode navigation_bringup --event-handlers console_direct+`; `colcon test-result --verbose` reported **517 tests, 0 errors, 0 failures, 0 skipped** before the Python-only scenario receipt update. The later full run also completed the product package tests. Logs: `/tmp/uav-mission-cut-colcon-test.log`, `/tmp/uav-mission-cut-test-result.log`.
- `test_mission_progress`: 13 deterministic cases including crossing/continuation order, crossing after leaving ball, identity reset, reverse/out-of-order/gap, sharp corner, loop/parallel branch, coincident PASS→STOP and measured STOP. `test_navigation_mode_progression`: five adapter boundary cases. Core integration verifies internal request 1→2 and exact receipt gating. These are component/model evidence.
- `python3 -m unittest tools.runtime.tests.test_runtime_contract.RuntimeContractTest.test_mission_scenario_reads_core_progress_receipt` passed. This checks the migrated SITL scenario reads Core GOAL/acceptance receipts instead of the adapter's old mission status field.
- Full `python3 -m unittest tools.runtime.tests.test_runtime_contract` passed **232 tests, one skipped** after updating a source-contract assertion to locate `runCycle(` independent of its stop-token signature. Log: `/tmp/uav-mission-cut-python-runtime-tests-final.log`.
- `python3 tools/check_mission_authority_cut.py` passed: no adapter MissionController construction/link, mission-file policy, or successor goal publisher in the product path. `python3 tools/validate_runtime_safety_ledger.py` and `git diff --check` passed after safety documentation change.

Full `colcon test` across all 23 packages completed in 2 min 15 s but returned nonzero. Aggregate `colcon test-result --verbose` reported 4,693 tests, 2 errors, 3,039 failures and 121 skipped. Twelve `px4_ros2_interface_lib` example packages failed lint; `px4_ros2_cpp` also had a `MissionExecutionTester.resumeFromLandedInRtl` expected-log mismatch plus missing integration/uncrustify results. The pinned submodule was unchanged by this cut. The full-suite failure is retained as evidence; it is not relabeled as a product PASS. Logs: `/tmp/uav-mission-cut-colcon-full-test.log`, `/tmp/uav-mission-cut-full-test-result.log`.

The focused SITL differential in `SITL_RESULTS.md` blocks a successful migration verdict despite component tests passing. No focused run is flight qualification.
