# H3 — adapter-state stale Hold

The repository has typed state-stale admission rejection and terminal safety transition coverage. A fresh controlled SITL case proving state age above 200 ms through `failNavigation()` to PX4 `AUTO_LOITER` has not yet run in this branch. Result: not run.
