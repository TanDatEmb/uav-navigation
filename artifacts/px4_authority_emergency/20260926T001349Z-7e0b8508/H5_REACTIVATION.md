# H5 — reactivation

Source increments the adapter activation identity, invalidates its command cache, and requires a newly admitted command. Executor callback generations also change across activation/deactivation. The long-World-stale → Hold-confirmed → healthy restoration → explicit reactivation SITL timeline has not yet run. Result: source/component contract present; runtime evidence pending.
