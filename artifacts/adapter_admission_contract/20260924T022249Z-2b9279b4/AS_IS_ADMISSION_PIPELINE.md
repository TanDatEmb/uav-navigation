# AS-IS adapter admission pipeline

`NavigationMode::onNavigationCommand()` is at `src/px4/px4_navigation_external_mode/src/navigation_mode_node.cpp:844-1188` in BASE. Approximate 345 source lines. It first collapses null message, `commandContractValid`, and `commandValidAt(now)` into one `bool valid`; a failed precheck retains the prior accepted command and logs only `valid=0 command_present=1` for a present message. The contract predicate checks frame, header/valid-until shape, world/state stamps, identities, role/status, finite PVAJ/yaw/time, and the certified continuation pair. `commandValidAt` additionally checks callback time against the inclusive `[header, valid_until]` interval, with `valid_until > header`.

After precheck, terminal/handover/failure state ignores later commands. Under `trajectory_mutex_`, health epoch, mode activation, Core session, request ordering, and world/state identity monotonicity are collapsed into four booleans and one generic session/epoch rejection. Stale odometry increments rejects and later calls `failNavigation`; nonincreasing sample ID rejects while retaining previous. Tracking envelope failure increments rejects and later calls `safetyStopNavigation`. Accepted command replaces `navigation_command_` via `transitionCertifiedCommand(..., kCommit)`, increments accepted/received counters, and refreshes receive time. A successful `NavigationCommandAdmission` is published only after commit. Terminal recovery may later set bounded recovery state.

The callback invokes ROS/steady `now` multiple times: precheck, odometry freshness, tracking provenance, commit receive stamp, admission receipt, and recovery deadline. Those are separate observations today; typed precheck will capture one callback time without altering later safety checks unless equivalent.
