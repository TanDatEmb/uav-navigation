# Decisions and evidence gates remaining

1. **E1 retention/acceptance policy.** Decide whether an earlier typed in-ball/forward-crossing observation may be consumed after the vehicle leaves the ball, and under what route-cursor distance, source-time, reverse, gap, reset and mission identity rule. The one observed crossing-first episode had 1.240 s until published readiness and later re-entry; it does not justify any timeout. First capture exact adapter receive and MissionController callback order in a focused scenario. `SPECIFICATION_GAP`, `RUNTIME_UNVERIFIED`.
2. **E2 public API owner.** State whether installed/exported `MissionController::onTrajectory` remains a supported external integration contract and whether its pre-stop rolling recovery is needed for future native flight. If legacy, version/deprecate separately; do not remove in this audit. Native committed BACKUP/emergency remains one-way. `SPECIFICATION_GAP`.
3. **E3 firmware/protocol.** Pin a product PX4 source/build input set, then define a fresh post-request AUTO_LOITER status and owned-mode deactivation witness, callback absence, safe retry timeout, operator/failsafe takeover and shutdown outcomes. `executor_in_charge=0` is **not** required for requested Loiter at the captured run firmware. Record VehicleCommand/ACK/ModeCompleted in a focused SITL trace. `EXTERNAL_CONTRACT_UNRESOLVED`.
4. **E4 temporal policy.** Capture paired world snapshot→recertification start/commit, runtime publish→adapter receipt/accept and status/authority timing under repeated representative load and controlled stale-world input. Decide an explicit reserve and whether recoverable command silence remains allowed; no continuous-heartbeat policy change before this. `RUNTIME_UNVERIFIED`.

Only an audit-only, no-behavior-change instrumentation/replay branch is recommended next. `SHADOW_REDUCER_READY=NO`; `AUTHORITY_MIGRATION_READY=NO`. No production reducer or policy implementation follows from these artifacts.
