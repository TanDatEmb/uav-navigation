# E2 — `MissionController::onTrajectory` provenance and reachability

`FACT_FROM_PINNED_PRODUCT_SOURCE` The public header `src/px4/px4_navigation_external_mode/include/px4_navigation_external_mode/mission_controller.hpp:57-65` still declares three `onTrajectory` overloads. `CMakeLists.txt:50-58` installs headers and exports the contract target. The implementation (`mission_controller.cpp:101-227`) admits a successful non-STOP callback from `Braking` back to `ExecutingWaypoint` before measured stop. The comment at `:154-157` explicitly calls this rolling local-planner recovery. A current repository-wide source search finds no **product** caller; current adapter `navigation_mode_node.cpp:1272-1287` calls `onNativeSafetyTrajectoryObserved` or `onNativeTrajectoryReady`, neither of which performs this Braking transition. Tests in `test_mission.cpp:1449-1557` still exercise the old callback/timers. Absence of a repository call is not proof that installed external consumers do not exist.

`FACT_FROM_GIT_HISTORY` Commit `eb63af169c2730521c0e82ec5650634391e7e718` (19 Aug, “Stabilize external mode continuous planning and safety handover”) introduced the callback with a real `PlannedTrajectory` subscription/call chain in `NavigationMode`. Commit `7ac87c3c398bca732e8c93453f45b6aec489e9b8` (20 Aug, “stabilize continuous UAV mission benchmark”) added the rolling-recovery branch and performance rationale. Commit `16613868860c02aec70f4f35b3c24278a747daec` (“replace navigation pipeline with SUPER planner”) removed the old subscribed trajectory path and replaced the product callback with `onNativeTrajectoryReady`. `git log -S` and `git blame` establish lineage; they do not establish an API deprecation decision. Commit `0930a8d5551479be6ec5d5c5674b29405641d2b1` later calls it a “public transport boundary” when validating role/kind pairs, supporting that the surviving API was consciously hardened, not accidentally deleted.

`FACT_FROM_PINNED_PRODUCT_SOURCE` Native runtime currently treats sampled BACKUP and emergency execution as one-way safety disposition until a certified measured stop (`execution_recovery_state.hpp:28-88`, `navigation_runtime_node.cpp:3792-3865,8180-8235`). A failed replacement while retained MAIN remains certified continues MAIN; that is not pre-stop recovery from committed BACKUP. The native adapter's `onNativeSafetyTrajectoryObserved` updates generic mission readiness but does not map a BACKUP sample to `MissionController::Braking`. No source producer→consumer path from native committed BACKUP/emergency to nominal MAIN before measured stop was found: **NO_PRODUCT_PATH_FOUND**, not `IMPOSSIBLE`. Runtime traces in this branch do not carry enough typed transition events to prove universal absence.

`SPECIFICATION_GAP` The public install/export contract does not state external ABI compatibility level, supported downstream callers, API deprecation/versioning or whether future pre-commit recoverable braking remains a required flight capability. A targeted workspace search found other repository/worktree/test copies, no independent downstream product caller; public install still prevents a safe “dead API” conclusion. The safety current/index targeted search found native committed BACKUP/emergency records but no explicit supersession/rejection of the `onTrajectory` rolling-recovery promise. Historical tests protect the callback's local behavior, not current native flight behavior.

Answers: (1) it originated in the older subscribed trajectory path; (2) yes, it predates native SUPER pipeline; (3–4) external support/ABI expectation unresolved despite exported header; (5) native product producer was replaced; (6) tests cover historical API behavior; (7) comment came from `7ac87c3c` benchmark continuity rationale; (8) no explicit ledger supersession found. No API was removed or changed.
