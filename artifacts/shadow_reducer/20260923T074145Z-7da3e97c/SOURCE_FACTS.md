# Pinned source facts

All source paths below refer to product commit `7da3e97cb399c2e39d62cfe60213a45e8a92300e` plus the guarded diagnostic hooks at `d926013e`.

- `FACT_FROM_TARGET_CODE`: `MissionController::update()` saves the previous measured position/time, updates `RouteProgress`, calls `measuredWaypointCrossingError`, then requires `progression_ready && inside && acceptance_ready` in the same callback. Crossing is not retained as a separate accepted mission fact. `src/px4/px4_navigation_external_mode/src/mission_controller.cpp:370-378,460-490,589-610`.
- `FACT_FROM_TARGET_CODE`: inside-ball acceptance checks ordered route projection; two-sample crossing additionally requires a finite previous/current position, sample gap ≤0.25 s, forward incoming tangent, segment-to-waypoint distance within radius, and ordered projection. `src/contracts/navigation_mission/src/route_progress.cpp:430-496`; `src/px4/px4_navigation_external_mode/include/px4_navigation_external_mode/mission_controller.hpp:125`.
- `FACT_FROM_TARGET_CODE`: PASS progression accepts a valid current MAIN continuation, certified suffix stop, coincident terminal hold, or initial PASS exception. STOP has measured speed/confirmation logic. `mission_controller.cpp:506-610`.
- `FACT_FROM_TARGET_CODE`: `NavigationModeExecutor::schedulePx4Hold` sets pending/in-flight; callback success/deactivation clears both while VehicleStatus confirmation is a separate update. Retry is conditional on pending and not confirmed. `src/px4/px4_navigation_external_mode/src/navigation_mode_node.cpp:3090-3110,3144-3180`.
- `FACT_FROM_PINNED_DEPENDENCY`: `px4_ros2::Result` in submodule `4a3370f084ac6f1ef001a4afa2b007845ffd0837`, `px4_ros2_cpp/include/px4_ros2/components/mode.hpp:30-59`, assigns `Success=0`, `Rejected=1`, `Deactivated=4`. The observed O1-R09 phase-4 `outcome=1` is rejection, not completion success.
- `FACT_FROM_TARGET_CODE`: adapter command age gate is 0.10 s; state age gate 0.20 s. `navigation_mode_node.cpp:148-161`. Core world freshness and command valid-until are separate contracts; `src/runtime/navigation_runtime/src/navigation_runtime_node.cpp:745-760,8603-8606`.
- `FACT_FROM_RUNTIME_TRACE`: 10 completed O1 episodes yielded 39 PASS acceptance and 10 STOP acceptance records. All PASS cases are ready-before-cross in the published experiment table; no crossing-first case was observed. Instrumentation artifact `O1_PASS_THROUGH_EVENTS.csv`, `EXPERIMENT_VERDICT.md`.
- `FACT_FROM_RUNTIME_TRACE`: W2-W5 each contain stale-world suspension, adapter lease expiry, Hold request/status, and later fresh world without observed old-session command resume. W1 is the fresh control. `O4_STALE_WORLD_EVENTS.csv` and normalized traces.
- `FACT_FROM_RUNTIME_TRACE`: O3 raw protocol has request, VehicleCommand/ACK and AUTO_LOITER status; no LOITER `ModeCompleted` in the six published episodes. One additional O1-R09 phase-4 callback is emitted specifically by `onPx4HoldHandoverCompleted` for the scheduled LOITER operation; `outcome=1` is `Result::Rejected` in pinned dependency `px4_ros2/components/mode.hpp:30-39`, not Success. It is outside the six published O3 episodes. `O3_HOLD_PROTOCOL_EVENTS.csv`, normalized `audit_events.jsonl`.
- `INFERENCE`: Source's callback-local crossing allows the ordering crossing→continuation to lose mission acceptance if no later valid crossing occurs. Model PT2 proves the target representation can retain the fact; it is not a reproduced runtime defect.
- `SPECIFICATION_GAP`: PX4 firmware authority semantics of `ModeCompleted`, operator/failsafe takeover, and real transport/scheduler latency tails remain unproven by these bags. E2 exported `onTrajectory` writer remains `ExternalRecoveryApi/UnknownOwner`.
