# Scenario traces

Evidence class: `FACT_FROM_CODE` describes inspected source path; `TEST_SUPPORTED` points to assertion sites read but not run; `RUNTIME_OBSERVED` is absent in this run; `INFERENCE/UNRESOLVED` marks reachability/behavior not proven.

| ID / scenario | Pre-state and event order | Branch / writes | Command and post-state | Status / evidence |
|---|---|---|---|---|
| S01 / Activate -> airborne/health/state/frame -> first command | mode inactive/checkpointed; no locally admitted command; runtime may have no fresh current epoch/world Events: mode activates and calls updateMission → MissionController enters WaitingForAirborne; if not (isArmed && measured local z>0.5), emits no goal → after airborne, MissionController enters ExecutingWaypoint and emits first goal → health/odometry ingress and optional stationary PX4-frame alignment → runtime solve/commit/sample/publish → PX4 adapter admits command and 50 Hz setpoint gate evaluates | No mission goal before airborne guard; after goal, stationary setpoint during bounded command acquisition; moving PVA only after required health, state, lease, identity and frame gates. | local command cache may be admitted; PX4 setpoint update called; actual PX4 application unknown | static-path-supported; MissionController unit test covers airborne gate; NavigationMode onActivate comment is broader than update guard/test and is preserved as conflict; [<code>E_MODE_ACTIVATE</code>](../evidence/refs.html#E_MODE_ACTIVATE), [<code>E_MISSION_FLOW</code>](../evidence/refs.html#E_MISSION_FLOW), [<code>E_FRAME</code>](../evidence/refs.html#E_FRAME), [<code>E_PUBLISH</code>](../evidence/refs.html#E_PUBLISH), [<code>E_SETPOINT</code>](../evidence/refs.html#E_SETPOINT), [<code>E_PROGRESSION_TEST</code>](../evidence/refs.html#E_PROGRESSION_TEST), [<code>E_ACTIVATE_COMMENT</code>](../evidence/refs.html#E_ACTIVATE_COMMENT), [<code>E_MISSION_UPDATE</code>](../evidence/refs.html#E_MISSION_UPDATE) |
| S02 / Nominal request -> solve -> validation -> commit -> sample -> PX4 boundary | current desired/executing identity and fresh world/state; active bundle absent or valid Events: PlanningKey snapshot → serial worker solves → candidate cert/identity checks → store commit immediate or stage future successor → command timer activates due successor → sampler samples role/time → final store exposure gate publishes → PX4 callback validates/cache then setpoint timer updates | failed/stale guard rejects or preserves prior active; staged remains non-executable until due activation | source proves each code boundary; no proof that PX4 accepted/applied the setpoint | static-path-supported; component tests support store and adapter substeps; no runtime trace; [<code>E_SCHEDULE</code>](../evidence/refs.html#E_SCHEDULE), [<code>E_WORKER</code>](../evidence/refs.html#E_WORKER), [<code>E_CANDIDATE</code>](../evidence/refs.html#E_CANDIDATE), [<code>E_COMMIT</code>](../evidence/refs.html#E_COMMIT), [<code>E_ACTIVATION</code>](../evidence/refs.html#E_ACTIVATION), [<code>E_PUBLISH</code>](../evidence/refs.html#E_PUBLISH), [<code>E_MODE_ADMISSION</code>](../evidence/refs.html#E_MODE_ADMISSION), [<code>E_SETPOINT</code>](../evidence/refs.html#E_SETPOINT), [<code>E_BUNDLE_TEST</code>](../evidence/refs.html#E_BUNDLE_TEST) |
| S03 / PASS_THROUGH request advances while old command is active | mission/wp/request N; fresh measured route progress; exact admitted continuation or certified suffix stop; runtime executing N Events: measured crossing/stop accepted → MissionController advances waypoint/request and emits goal N+1 → NavigationMode retains exact still-valid old command if retention predicate allows → runtime accepts new desired identity, may keep executing predecessor while successor is staged → activation swaps timeline/executing identity; pending pointer consumed if suffix deferred | old command is never relabeled; old lease/identity gates remain; suffix ownership permits one same-mission pending goal | mission request may be N+1 while executing identity is N until commit boundary; no atomic cross-process transaction | static-path-supported; progression and stale-witness tests support pieces; [<code>E_MISSION_FLOW</code>](../evidence/refs.html#E_MISSION_FLOW), [<code>E_MISSION_EVENT</code>](../evidence/refs.html#E_MISSION_EVENT), [<code>E_GOAL</code>](../evidence/refs.html#E_GOAL), [<code>E_ACTIVATION</code>](../evidence/refs.html#E_ACTIVATION), [<code>E_PROGRESSION_TEST</code>](../evidence/refs.html#E_PROGRESSION_TEST), [<code>E_MISSION_TEST</code>](../evidence/refs.html#E_MISSION_TEST) |
| S04 / MAIN -> BACKUP while nominal solve is in flight; late nominal result | active MAIN+BACKUP timeline; normal renewal solve active Events: sampler reaches bundle-declared BACKUP interval → exact role sample updates ExecutionEpisode recovery → normal solve result later rechecks request/execution/timeline/episode owner → commit guard rejects nominal result unless current recovery permits it | worker only serializes planner jobs; sampling still runs; stale candidate is discarded/retained decision uses exact timeline snapshot | BACKUP remains authority if result superseded; source path exists. Test covers stale episode mutator and worker cancellation/identity but not full timer race as one integrated trace | static-path-supported; integrated race trace unresolved; [<code>E_RECOVERY_USE</code>](../evidence/refs.html#E_RECOVERY_USE), [<code>E_PUBLISH</code>](../evidence/refs.html#E_PUBLISH), [<code>E_COMMIT</code>](../evidence/refs.html#E_COMMIT), [<code>E_EPISODE_TEST</code>](../evidence/refs.html#E_EPISODE_TEST), [<code>E_WORKER_TEST</code>](../evidence/refs.html#E_WORKER_TEST) |
| S05 / Emergency selected; emergency certification fails | terminal MAIN/anchor watcher or exceeded command guard; exact bundle active Events: runCycle prepares measured-state emergency candidate → candidate certification/boundary may fail → request/execution snapshot is rechecked → current-owner failure applies kEmergencyCertificationFailed and failClosed | superseded callback is discarded first; current callback fail-closes; no emergency trajectory is synthesized | runtime authorization rejected/terminal; downstream adapter safety stop and Hold are separate transitions | static-path-supported; exact end-to-end event not runtime-observed; [<code>E_RECOVERY_USE</code>](../evidence/refs.html#E_RECOVERY_USE), [<code>E_RECOVERY_HELPER</code>](../evidence/refs.html#E_RECOVERY_HELPER), [<code>E_EPISODE_TEST</code>](../evidence/refs.html#E_EPISODE_TEST), [<code>E_PUBLISH</code>](../evidence/refs.html#E_PUBLISH) |
| S06 / Measured stop -> stopped recovery -> retry/replan or timeout | BACKUP/EMERGENCY suffix owns timeline, runtime recovery tracks suffix Events: measured state/certificate stop evidence checked → Episode transitions to StoppedRecovery/StoppedHold and remembers restart-from-rest → worker schedules plan-from-rest or deferred handoff → runtime stopped-recovery deadline can fail closed → PX4 adapter has separate completed-command 5 s planner recovery clock | UNKNOWN or stale/mismatched evidence never authorizes restart; runtime and PX4 recovery timers are distinct identities/clocks | new MAIN commit may resume; otherwise runtime fail-closed or adapter Hold on its own timeout branch | static-path-supported; exact integrated timeout race unresolved; [<code>E_RECOVERY_USE</code>](../evidence/refs.html#E_RECOVERY_USE), [<code>E_EPISODE</code>](../evidence/refs.html#E_EPISODE), [<code>E_RUNTIME_TIMEOUT</code>](../evidence/refs.html#E_RUNTIME_TIMEOUT), [<code>E_MODE_RECOVERY</code>](../evidence/refs.html#E_MODE_RECOVERY), [<code>E_EPISODE_TEST</code>](../evidence/refs.html#E_EPISODE_TEST) |
| S07 / Terminal STOP -> measured settling -> waypoint acceptance -> mission completion | STOP waypoint active; candidate endpoint/terminal status and mode command may be current Events: trajectory endpoint can be published COMPLETED → measured position/speed and confirmation/dwell evaluated → nonterminal stop accepts waypoint then publishes next goal; terminal stop emits Complete after hold_s → mode sets mission_terminal/handover and publishes completion Bool/status; executor requests Hold | trajectory-ended, terminal hold pending, measured settle, accepted waypoint, mission Complete, Hold requested and Hold confirmed are distinct | mission completion status published and setpoint/hold behavior persists until mode/status transition | static-path-supported and several unit tests cover distinct predicates; [<code>E_MISSION_FLOW</code>](../evidence/refs.html#E_MISSION_FLOW), [<code>E_MISSION_EVENT</code>](../evidence/refs.html#E_MISSION_EVENT), [<code>E_MISSION_TEST</code>](../evidence/refs.html#E_MISSION_TEST), [<code>E_PROGRESSION_TEST</code>](../evidence/refs.html#E_PROGRESSION_TEST) |
| S08 / PASS_THROUGH and STOP coincident/zero-length boundary | route has a PASS_THROUGH immediately followed by coincident STOP Events: pass-through current request reaches stop boundary with terminal/safety marker → coincident-stop helper keeps terminal readiness pending → measured STOP settling then emits next goal or completion | special semantics exist; do not collapse pass acceptance and STOP acceptance; outside exactly-coincident path, general zero-length route semantics need additional audit | next STOP owns measured hold/terminal acceptance | static-path-supported for coincident pass-through STOP; broader zero-length cases unresolved; unit test covers helper case; [<code>E_MISSION_FLOW</code>](../evidence/refs.html#E_MISSION_FLOW), [<code>E_MISSION_TEST</code>](../evidence/refs.html#E_MISSION_TEST) |
| S09 / World revision or localization epoch changes across solve/commit/publish | solve captures world identity/revision and localization epoch Events: new world snapshot or higher localization epoch arrives before candidate/command final gate → optimistic commit/exposure rechecks identity and current freshness → epoch reset drains/cancels/invalidate; world change requires recertification | old candidate rejected or old command temporarily suspended only under revalidation path; failed current owner fails closed | old lineage cannot be exposed once mismatching identity gate is observed | static-path-supported; store/epoch tests exercise components; no end-to-end runtime trace; [<code>E_COMMIT</code>](../evidence/refs.html#E_COMMIT), [<code>E_PUBLISH</code>](../evidence/refs.html#E_PUBLISH), [<code>E_EPOCH</code>](../evidence/refs.html#E_EPOCH), [<code>E_BUNDLE_TEST</code>](../evidence/refs.html#E_BUNDLE_TEST), [<code>E_TERMINAL_TEST</code>](../evidence/refs.html#E_TERMINAL_TEST) |
| S10 / Command lease expiry, stale state/health, PX4 frame reset | active or cached command and previous source/receive/frame witnesses Events: bundle/message/state/health lease expires or PX4 reset counters advance → runtime publication gate and mode setpoint gate reevaluate current freshness/frame → frame invalidated until stationary paired alignment witness | runtime fail-closed or world suspend; PX4 stationary hold then bounded terminal recovery only on exact identity; otherwise safety stop/Hold; stale/wrong frame command rejected | no old moving setpoint is authorized by stale cache; applied vehicle state unknown | static-path-supported; progression tests cover wrong epoch/stale health/odom; frame runtime trace absent; [<code>E_PUBLISH</code>](../evidence/refs.html#E_PUBLISH), [<code>E_MODE_ADMISSION</code>](../evidence/refs.html#E_MODE_ADMISSION), [<code>E_SETPOINT</code>](../evidence/refs.html#E_SETPOINT), [<code>E_FRAME</code>](../evidence/refs.html#E_FRAME), [<code>E_PROGRESSION_TEST</code>](../evidence/refs.html#E_PROGRESSION_TEST) |
| S11 / Operator takeover, deactivate/reactivate, late callback or pending request | mode/runtime active, possible pending worker result or deferred goal Events: operator mode exit/deactivate invalidates local command and MissionController checkpoints → executor clears hold tracking; reactivation resets per-mode latches and resumes checkpoint with a new request → runtime receives status/goal identity callbacks and rejects stale epochs/identity where guards apply | deactivation callback cleanup is code-backed; exact propagation/cancellation of every runtime pending item across process handover requires more review; stale callback behavior depends identity guards | mode may restart mission checkpoint with new request; no blanket proof all remote pending queue entries cleared | static-path-supported for mode checkpoint; cross-process pending-state drain unresolved; [<code>E_MODE_ACTIVATE</code>](../evidence/refs.html#E_MODE_ACTIVATE), [<code>E_MISSION_TEST</code>](../evidence/refs.html#E_MISSION_TEST), [<code>E_MODE_ADMISSION</code>](../evidence/refs.html#E_MODE_ADMISSION), [<code>E_GOAL</code>](../evidence/refs.html#E_GOAL), [<code>E_WORKER_TEST</code>](../evidence/refs.html#E_WORKER_TEST) |
| S12 / PX4 Hold requested -> API result -> VehicleStatus confirmation | request position control or Complete; stationary setpoint input active; executor hold flags clear Events: schedule AUTO_LOITER → API callback returns result → VehicleStatus reports AUTO_LOITER or not → 50 ms timer retries only if pending and not confirmed | API failure keeps pending and sets steady retry +250 ms; API Success/Deactivated clears pending; only observed AUTO_LOITER confirms and clears both | source has a path where API success precedes status confirmation and retry predicate is disabled while pending=false; runtime evidence needed to determine normal callback/status relationship | static-path-supported; confirmation semantics are source-backed; API-success-without-status liveness is conditional/unresolved and untested here; [<code>E_HOLD</code>](../evidence/refs.html#E_HOLD), [<code>E_SETPOINT</code>](../evidence/refs.html#E_SETPOINT) |
