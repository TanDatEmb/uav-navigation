# Cross-object authority findings

These findings identify discussion points, not proposed replacement architecture. A distributed representation is not automatically a defect; identities, lifetimes and serialization can make it legitimate.

| ID / class / priority | Decision and participating fields | Evidence for overlap | Mechanism that can make it valid | Synchronization | Demonstrated effect / risk hypothesis | Test and gap | Evidence |
|---|---|---|---|---|---|---|---|
| FIND-01 / CONDITIONAL / high | Mission progress and command execution identity are owned in separate processes and can temporarily name different request generations. Fields: O_MISSION:F_WAYPOINT, O_MODE:F_MODE_COMMAND, O_RUNTIME:F_DESIRED_GOAL, O_RUNTIME:F_EXECUTING_GOAL, O_PENDING_GOAL:F_PENDING_GOAL, O_TIMELINE:F_TIMELINE | NavigationMode retains old accepted command without relabeling and runtime separates active_goal_ from executing_goal_; pending goal is a single exact-pointer suffix handoff; timeline activation changes executing identity. | Distinct request/goal/localization/bundle identities, leases, and measured continuation/suffix-stop witnesses make this a deliberate asynchronous handoff; no one cross-process transaction exists. | trajectory_mutex_ within mode; runtime lock order and store CAS within runtime; ROS message delivery between processes is asynchronous. | A delayed/reordered goal or command can temporarily leave mission desired progress ahead of physical command owner; source contains identity guards but integrated delayed/reorder coverage is incomplete. | test_navigation_mode_progression tests predecessor/current-request and stale witness behavior; runtime worker/store tests cover independent sub-boundaries; no integrated ROS two-process trace. | [<code>E_RUNTIME_FIELDS</code>](../evidence/refs.html#E_RUNTIME_FIELDS), [<code>E_MISSION_EVENT</code>](../evidence/refs.html#E_MISSION_EVENT), [<code>E_ACTIVATION</code>](../evidence/refs.html#E_ACTIVATION), [<code>E_PROGRESSION_TEST</code>](../evidence/refs.html#E_PROGRESSION_TEST), [<code>E_BUNDLE_TEST</code>](../evidence/refs.html#E_BUNDLE_TEST) |
| FIND-02 / CONDITIONAL / high | Hold handover uses separate mode request, executor scheduling, API completion, and observed vehicle navigation state flags. Fields: O_MODE:F_HANDOVER_MODE, O_EXECUTOR:F_HOLD_EXECUTOR, O_MISSION:F_TERMINAL_HOLD | Only VehicleStatus AUTO_LOITER sets px4_hold_confirmed_; schedule API Success/Deactivated clears pending and in-flight without setting confirmed, while retry timer requires pending && !confirmed. | PX4 mode API Success may be contractually followed by state transition; current source alone does not establish timing or guarantee. Failure results do retry every 250 ms; no maximum retry count is found. | Executor timer/subscription/API callbacks; source does not show a shared explicit mutex for these flags; callback-group/executor serialization details require verifying PX4 library implementation. | If API reports Success but AUTO_LOITER is never observed, source path stops retrying while confirmation remains false; this is a liveness risk hypothesis, not observed vehicle behavior. | No targeted executor handover test located among reviewed tests; need controlled adapter test/trace capturing API result and VehicleStatus order. | [<code>E_HOLD</code>](../evidence/refs.html#E_HOLD), [<code>E_PX4_MAIN</code>](../evidence/refs.html#E_PX4_MAIN) |
| FIND-03 / CONDITIONAL / medium | Runtime recovery and PX4 adapter recovery each have separate state, timer, identity, and terminal effects. Fields: O_EPISODE:F_EXEC_RECOVERY, O_MODE:F_PLANNER_RECOVERY_MODE, O_MODE:F_SUFFIX_HANDOFF, O_MODE:F_HANDOVER_MODE | ExecutionEpisode owns runtime TrackBackup/Emergency/StoppedRecovery/Px4Hold; NavigationMode owns a separate 5 s completed-command recovery episode and its own suffix handoff/hold flags. | The two processes handle different lifetimes and message boundaries; fields may be projections/replicas keyed by distinct goal/request/bundle identities, not duplicate authority. | Message status/command contracts and exact identity lease checks; there is no shared cross-process transaction or common timer. | Same episode can appear as runtime stopped recovery while PX4 adapter awaits a new command; timing mismatch can cause stale recovery or early handover if message identity changes; integrated sequence remains unobserved. | Runtime episode and mode progression tests cover components; no paired-runtime/PX4 adapter scenario test in this review. | [<code>E_EPISODE</code>](../evidence/refs.html#E_EPISODE), [<code>E_MODE_FIELDS</code>](../evidence/refs.html#E_MODE_FIELDS), [<code>E_SETPOINT</code>](../evidence/refs.html#E_SETPOINT), [<code>E_EPISODE_TEST</code>](../evidence/refs.html#E_EPISODE_TEST), [<code>E_PROGRESSION_TEST</code>](../evidence/refs.html#E_PROGRESSION_TEST) |
| FIND-04 / CONDITIONAL / high | SITL tracking/health response experiment activation is controlled by use_sim_time, and zero coefficients do not mean all bypass behavior is disabled. Fields: O_RUNTIME:F_TRACKING_EXPERIMENT, O_MODE:F_TRACKING_EXPERIMENT, O_CONTRACT:TrackingExperimentPolicy, O_RUNTIME:F_RUNTIME_TRACKING_EXPERIMENT | loadTrackingExperimentPolicy sets enabled=use_sim_time; when all tracking coefficients are zero, trackingGateEnabled() is false, so simulated mode sets suppress_braking=true and suppress_estimator_health_response=true. Both runtime and PX4 mode read the policy. Source emits an explicit SITL increased-collision-risk/not-flight-qualification warning. Config comments say a zero tracking envelope disables tracking/health gates, while YAML zeros are present. Runtime runCycle selects the experiment acceptance result in place of the path-relative gate when enabled; `experimentPermitsTracking` allows out-of-envelope tracking if suppress_braking is true. | Combined launch defaults use_sim_time=false; if selected run passes false, suppress flags stay false. For simulated profile use_sim_time=true, code sets suppress flags true despite zero coefficients. The actual launch args/runtime profile used by a live deployment are not known in this static audit. Combined launch argument use_sim_time defaults false, but the user-selected SITL launch may pass true; no active launch values were observable. | Immutable policy loaded at process construction; runtime and PX4 mode are separate parameterized processes and can receive different parameter sets. | Under SIM_TIME with zero tracking coefficients, runtime/mode may suppress the geometric tracking-brake/estimator-health response; the experiment logs characterize this as increased collision risk. This is code-proven for that parameter valuation, not an assertion that the live profile used it. | Reviewed mode fixture asserts tracking_experiment_.enabled and suppress_estimator_health_response are false for its test configuration; no test specifically covers use_sim_time=true with zero coefficients across both processes. | [<code>E_TRACKING_POLICY</code>](../evidence/refs.html#E_TRACKING_POLICY), [<code>E_TRACKING_CONFIG</code>](../evidence/refs.html#E_TRACKING_CONFIG), [<code>E_EXTERNAL_MODE_CONFIG</code>](../evidence/refs.html#E_EXTERNAL_MODE_CONFIG), [<code>E_TRACKING_RUNTIME</code>](../evidence/refs.html#E_TRACKING_RUNTIME), [<code>E_TRACKING_MODE_BYPASS</code>](../evidence/refs.html#E_TRACKING_MODE_BYPASS), [<code>E_PROGRESSION_TEST</code>](../evidence/refs.html#E_PROGRESSION_TEST), [<code>E_RUNTIME_TRACKING_GATE</code>](../evidence/refs.html#E_RUNTIME_TRACKING_GATE), [<code>E_EXPERIMENTAL_TRACKING_HELPER</code>](../evidence/refs.html#E_EXPERIMENTAL_TRACKING_HELPER), [<code>E_LAUNCH_DEFAULT</code>](../evidence/refs.html#E_LAUNCH_DEFAULT) |
