# Phase C — PX4 authority handover

**BLOCKED / NOT STARTED.** A2 observed the adapter's existing fail-close
request after stale odometry; this does not establish the full scheduleMode,
callback, VehicleStatus, retry, operator, or failsafe protocol. No Hold
policy or adapter state was changed.
