# E2 — braking API ownership and provenance

**Current native policy:** source-owned sampled BACKUP/emergency safety execution is one-way until certified/measured stop in the current native path; failed replacement while retained MAIN remains safe continues MAIN, which is a different case. `MissionController::onTrajectory()` still permits `Braking→ExecutingWaypoint` on successful non-stop trajectory (`mission_controller.cpp:101-227`). The native adapter calls `onNativeSafetyTrajectoryObserved`/`onNativeTrajectoryReady` (`navigation_mode_node.cpp:1272-1287`), not the old callback. No current producer→consumer path from native committed BACKUP/emergency to pre-stop nominal recovery was found. This is `NO_PRODUCT_PATH_FOUND`, not universal impossibility.

**History:** `eb63af16` introduced the callback with a real subscribed PlannedTrajectory consumer. `7ac87c3c` added rolling recovery from Braking for continuous benchmark/mission behavior. `16613868` replaced the subscribed path with the native SUPER trajectory path. `0930a8d5` subsequently hardened role/kind validation on the still-public callback, so the API was consciously maintained at least at that point. The header declares overloads and the contract library installs/exports headers/target (`src/px4/px4_navigation_external_mode/CMakeLists.txt:50-61`). Existing tests in `test_mission.cpp:1449-1557` exercise the legacy behavior, not current native reachability.

**Consumer search:** all 21 current local heads/remotes/tags were searched with `git grep`; no current non-test `onTrajectory` call outside its implementation was found. `git log -S` located historical callsites and removal. Authenticated GitHub code search scoped to `user:TanDatEmb` returned only this repository's declaration/implementation/tests and unrelated planner `onTrajectory` identifiers; no independent exposed downstream caller was found. No arbitrary user filesystem or unrelated local repository was scanned. Search absence does not prove no private/installed downstream consumer. No API compatibility/deprecation policy or product-owner decision was located in targeted docs/safety history.

**Disposition:** `API_POLICY_OWNER_DECISION_REQUIRED`. The shadow model may describe *current native execution* without adding a recoverable-braking state solely to mirror this exported legacy API, provided its source stream records native role/safety transitions. External API compatibility and future recovery capability remain separate owner decisions. No API or flight behavior was changed.
