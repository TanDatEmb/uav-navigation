# O3 — requested PX4 Hold runtime order

**Verdict: `O3_REQUIRES_INSTRUMENTATION` for a deterministic shadow event stream; `O3_PARTIAL` as current runtime evidence.** The associated run firmware source narrows semantics, but full binary source reproducibility and product firmware baseline remain open as stated in `O3_PX4_RUN_FIRMWARE_PROVENANCE.md`.

`FACT_FROM_PINNED_DEPENDENCY` At gitlink `4a3370f...`, `mode_executor.cpp:48` publishes to `/fmu/in/vehicle_command_mode_executor`; `:125-260` sends `VEHICLE_CMD_SET_NAV_STATE`, maps matching accepted ACK to successful request dispatch and activates `ScheduledMode`; `:484-519` delivers callback for matching `ModeCompleted` while active or cancellation/deactivation. VehicleStatus is processed separately (`:361-419`). `FACT_FROM_RUN_FIRMWARE_CHECKOUT` Associated PX4 `Commander.cpp:936-946` ACKs accepted *intention*, while actual nav state is selected/published later (`:2367-2373`); `ModeManagement.cpp:415-435` can leave executor scheduling charge=1 on executor-requested Loiter. `Navigator::run` selects Loiter for `AUTO_LOITER`, whose `Loiter::on_active` persists setpoint; ModeCompleted producers in `navigator_main.cpp:1602-1608` are finite modes, with no Loiter publisher found. This source interpretation applies to the associated checkout with the provenance caveat, not a global firmware contract.

`FACT_FROM_EXISTING_RUNTIME_ARTIFACT` `O3_PX4_HOLD_EVENTS.csv` contains 617 observed rows from 18 mixed runs (14 historical plus four focused smoke runs): 20 adapter Hold-request log lines, 13 Hold completion callbacks (all `Deactivated`), 146 runner VehicleStatus changes, 333 runner ACK observations and nine **runner monitor** `px4_hold_handover_requested` events (not adapter schedule calls). Ninety-two ACK observations have command code 100001, but runner records only command/result and cannot attribute them to adapter Hold versus another producer. The runner recorded eight `external_mode_completed(nav_state=26,result=0)` events. Its subscription at `external_mode_scenario.py:1594-1624` drops ModeCompleted events whose `nav_state` is not the owned external-mode ID. The 14 historical bags omit raw VehicleCommand, ACK and ModeCompleted. A new focused sidecar bag captures all protocol topics. It contains no Loiter ModeCompleted in its 42.6 s window; its one ModeCompleted is TAKEOFF (`nav_state=17`). This is one SITL observation, not proof Loiter never emits completion. AUTO_LOITER is observed, including `nav_state=4, executor_in_charge=1` at simulation time 259.532 s in run `...247206`; charge=0 is not a valid required Hold witness.

Current adapter at `navigation_mode_node.cpp:2846-2855` sets a plain Hold-confirmed boolean on any AUTO_LOITER status, with no request generation or source/receive freshness. At `:2894-2929`, a successful or `Deactivated` scheduled-mode callback clears `pending` even if status never confirmed; a missing callback can leave `in_flight` true indefinitely. Source-derived retry-stop gaps remain reachable as protocol paths. The focused sidecar bag correlates one exact mode-executor request→ACK→fresh status by command code, Loiter parameter and component 1001, but still lacks adapter attempt generation and callback state. It does not prove universal retry-stop behavior. The one simulation-pause run is infrastructure-invalid for timing claims.

Required success witness for future policy analysis: fresh post-request `VehicleStatus.AUTO_LOITER` from PX4 with monotone PX4 boot/source time and adapter receive time, tied to current request generation by bounded temporal ordering; no intervening operator/failsafe override. No PX4 transaction ID exists in the inspected request/status pair, so a pilot choosing Loiter concurrently remains an attribution ambiguity. Classify that as `UNKNOWN_CAUSE`, not requested-Hold success. ACK proves accepted intention only. ModeCompleted and `Deactivated` do not prove current Loiter occupancy. Failsafe or pilot change should be separately observed as external takeover; exact retry/termination policy requires owner decision. Raw protocol topics can be captured without product edits using `tools/record_hold_topics.sh`, after checking topic inventory; the audit harness was run successfully once after two recorder setup failures; its raw bag and normalized events are indexed below. Focused repetitions must count ACK→Status→callback, ACK→callback→Status, no status, already-Loiter, repeat request, operator and failsafe orders before adapter redesign.

## Focused direct protocol capture

`FACT_FROM_RUNTIME_TRACE` Four new smoke SITL attempts used the same product TARGET, associated PX4 SHA/dirty patch and binary hash. All evaluator verdicts are `BLOCKED` under an existing `tracking_experiment=relaxed` configuration; no gate was changed for this audit and none is qualification evidence. The first recorder missed PX4 topics after the run stopped. The second recorder saw topic names but could not deserialize `px4_msgs` because the product install was not sourced. The third captured request-adjacent ACK/status but omitted the executor-specific command topic. The fourth ran `tools/record_hold_topics.sh` with the product install sourced, subscribed to all seven requested topics before the Hold event, and produced `O3_FOCUSED_PROTOCOL_EVENTS.csv` from its raw MCAP (SHA-256 `d3687764fd5e121e2780736a83d9198d4a3bfaa091a024eca42f9e3307871a2c`). Run: `external-mode-check-20260923T034522-65026`, metadata SHA-256 `b81ab7306d0e820fcaf603c0f0c5a2e0fc411bec618974a91c039e79a144f5d6`, report SHA-256 `a1fe580f93743bc1ae18d8d4760ce1ba34fee7f47350ece4673107b749f99d36`. Raw bag is ignored at `.artifacts/audit_runtime_observability/raw/hold_focus_20260923T0345_complete_topics/hold_protocol_bag/`; its derivative CSV is committed.

| Event | Observer wall ns in same bag | PX4 boot us | Identity/result |
|---|---:|---:|---|
| Mode executor VehicleCommand | 1790135180363112888 | command timestamp 0 | `command=100001`, `param1=4` (AUTO_LOITER), `source_component=1001` |
| VehicleCommandAck | 1790135180374250892 | 34620000 | `command=100001`, `result=0` (accepted), `target_component=1001` |
| Next fresh AUTO_LOITER status | 1790135180389193102 | 34632000 | `nav_state=4`, `executor_in_charge=1`, `failsafe=false` |

The same recorder sees status `nav_state=26` immediately before this transition. Bag observer ordering is request→ACK→status; request→ACK observer difference is 11.138 ms, request→status 26.080 ms. These are **recorder arrival deltas**, not transport or executor callback latency. PX4 boot stamps place ACK→AUTO_LOITER status 12 ms apart in its own domain. The adapter log requests Hold at wall 1790135180.362338 s, consistent with the exact executor command but only a supporting log witness. The bag has three executor commands total (two earlier External Mode requests and this Loiter request), nine ACKs, 79 VehicleStatus messages, one TAKEOFF ModeCompleted and no Loiter ModeCompleted. AUTO_LOITER continues in the captured status stream with executor charge 1 and later charge 0. No later Loiter request appears before recorder stop.

This one direct permutation closes **existence** of request→ACK→AUTO_LOITER occupancy for this run and refutes any rule requiring executor charge 0 at first Hold status. It does not tell when the adapter processed that status, whether its `pending/in_flight` flags cleared for the correct attempt, whether retry would occur under status loss, or whether an operator could have chosen Loiter concurrently. Three earlier smoke attempts did not yield a complete sidecar protocol bag; they are not counted as three successful protocol repetitions. Required other orderings and deactivation/failsafe injection remain unobserved.
