# O1 — PASS_THROUGH callback and ordering

**Verdict: `O1_REQUIRES_INSTRUMENTATION`.** `FACT_FROM_AUDIT_REPLAY` The previous 14 TARGET runs contain 32 distinct accepted PASS waypoint episodes and 65 pinned-geometry sample opportunities. Episode CSV collapses repeated opportunities. First opportunity: 28 had a compatible true continuation **published** before it, three are waypoint-zero/initial-handoff or otherwise insufficient, and one non-initial episode had an in-ball position before a future true continuation. All 32 *first* opportunities are `CURRENT_IN_BALL`; none is a first two-sample outside-ball skip. This is a property of these traces, not a product-wide distribution.

`FACT_FROM_TARGET_CODE` `MissionController::update` takes its previous/current position at `mission_controller.cpp:363-371`, updates route progress and calls the pinned `measuredWaypointCrossingError` at `:451-477`, then combines acceptance with continuation/suffix gates at `:550-579`. The adapter snapshots a compatible admitted command at `navigation_mode_node.cpp:1378-1409`, calls `update` at `:1463-1464`, and may call `updateMission` immediately from accepted continuation command callback at `:1328-1335` as well as its timer. Thus the recorder's odometry sample cadence is not the callback cadence. `RouteProgress` returns an in-ball result only on the route-ordered branch; a two-sample outside-ball result additionally requires forward motion and gap ≤0.25 s (`route_progress.cpp:430-490`). Product `MissionController` supplies **callback-time** gap; offline replay supplies source-time gap, so even pinned geometry yields opportunities rather than exact callback results. The replay uses the exact target source, not a separate geometry approximation. Existing golden vectors in `test_mission_contract.cpp:240-255,280-302` cover forward skip, reverse/lateral miss and self-crossing rejection; this branch reruns a focused audit fixture.

Selected episode `...015008-233245`, localization epoch `90695073308889`, waypoint 3/request 6: first in-ball source time 95.732 s, error 0.799 m, published continuation false; it left the ball after 96.372 s. First producer-side true continuation has command source stamp 96.972 s, 1.240 s after first observation. Pinned progress replay at these source instants changes monotonic route arc 36.2048→36.3491 m (**0.1443 m**); measured odometry path length over 63 samples is **0.55445 m**. It re-entered at 97.912 s and was accepted at 97.916 s. These are source-time differences; receive-time delay is **unobservable**. No retention threshold follows from one episode. `CROSSING_LOST_CANDIDATE=0 observed`, and `lost crossing reproduced=NO` because the observed accepted crossing is a re-entry.

For every episode, `O1_PASS_THROUGH_EPISODES.csv` records mission/request/epoch, first geometry kind, readiness producer time, acceptance time, and missing callback fields. The producer-side classification is **not** exact `READY_SAME_SAMPLE`, `CROSSING_BEFORE_READY` or `IDENTITY_INVALIDATED` at `MissionController`: callback ordering and admitted command state are absent. `N valid product callback crossings=UNOBSERVABLE`; `N sample-level valid RouteProgress opportunities=65`; `N first two-sample skipped-ball crossings=0`; `N exact callback insufficient=32`. No localization-reset crossing episode or high-speed skipped-ball runtime case was captured.

Missing exact witnesses: adapter odometry callback entry/source sequence; each `updateMission` invocation and previous/current odometry sequence; computed crossing kind/error; same-update continuation identity/readiness; mission result and successor execution cutover. See signal group O1 in `MINIMAL_INSTRUMENTATION_SPEC.md`. A focused crossing-first run must demonstrate departure before readiness and distinguish re-observed, invalidated and silently lost crossing. Until then the physical retention contract and target shadow input stream remain open.
