# O4 — stale world, recertification and downstream lease

**Verdict: `O4_REQUIRES_INSTRUMENTATION`; stale-world events observed: 0 in 13 planner-traced TARGET runs.** The fourteenth run has no useful planner command trace. There is no deterministic stale-world input gap in the existing exact-TARGET captures and no safe mapping-input pause/drop fault hook in `tools/runtime/` was found. A historical artifact with another source SHA cannot prove this TARGET path. No freshness threshold was changed.

`FACT_FROM_TARGET_CODE` The runtime checks world source freshness at planner cycle (`navigation_runtime_node.cpp:3670-3689`), command callback (`:7704-7719`) and final authorization (`:8733-8739`). A failed check calls `suspendCommandForWorldFreshness` (`:2983-3004`), retains the exact generation but makes command unavailable, so the command timer does **not** publish. A later mapping transaction can validate the retained exact bundle and increment `world_freshness_command_recovery_count` at `:1270-1291`. Adapter receive lease is 100 ms (`navigation_mode_node.cpp:2639-2648`), separate from command source/valid-until and odometry freshness. Runtime subscribes to `NavigationModeStatus` (`navigation_runtime_node.cpp:1609-1612,2283-2350`) for mission status, but no direct PX4 VehicleStatus/confirmed-Hold transaction witness is present in this producer. Therefore the exact question “may runtime resume after downstream authority transferred?” cannot be settled by current trace or source-only coincidence; a terminal mode status might clear execution, but the joint ordering is unobserved.

`FACT_FROM_TARGET_CODE` `active_revalidation_us` starts at `navigation_runtime_node.cpp:970` **after** pending validation has ended, covers active retained-candidate proof/validation and related checks, and ends at `:1130` **before** publication-finalization/commit. It excludes mapping callback queue wait, mapping/backend snapshot creation, world publication lock/commit, command timer wait, ROS publish, DDS, adapter callback queue and setpoint use. `mapping_callback_total_us` covers the callback envelope through finalization (`:1320-1328`), still excludes upstream source gap and downstream command stages. In `O4_WORLD_RECERTIFICATION_LATENCY.csv`, multiwaypoint run `...247206` has active revalidation N=2,846, p99=6.519 ms, max=10.499 ms; whole mapping callback N=2,512, p99=100.649 ms, max=124.723 ms. These are diagnostic duration samples, not event-paired recertification recoveries or a WCET. Do not add them as independent same-event stages.

`FACT_FROM_EXISTING_RUNTIME_ARTIFACT` Final suspend/recovery counters are zero across 13 planner-traced runs. Thus `last valid world→first stale rejection→last command→adapter receive-lease expiry→Hold→new world→recertification→resume` has **no observed instance**. `RECERTIFIED_BEFORE_COMMAND_LEASE_EXPIRY`, `RECERTIFIED_AFTER_LEASE_EXPIRY`, `ADAPTER_HANDOVER_BEFORE_RECERTIFICATION`, `ACTIVE_BUNDLE_REVOKED`, `BRAKE_COMMITTED`, `PX4_HOLD_REQUESTED`, `RECOVERY_WITHOUT_DOWNSTREAM_AUTHORITY` all have N=0 **observed**, not proof of impossibility. `Runtime-resume-after-adapter-handover observed=NO`; determinability under current trace=NO.

The required inequality is `source-gap detection + mapping queue + snapshot + revalidation + commit + command-timer wait + publication + DDS + adapter queue < remaining receive lease` **for a continuity policy**. Current 500 ms world freshness gate can stop command output well after the last observation; the 100 ms adapter lease then has its own age origin. No measured worst credible reserve is available. A focused harness must create a mapping-input gap without changing freshness/lease thresholds, retain world/bundle identity and capture every boundary in the matrix. At least ten identical-provenance repetitions would be useful *after* the missing witnesses and safe fault injection exist. `Continuous heartbeat` verdict: `INSUFFICIENT_EVIDENCE`; command silence currently means recoverable world suspension while adapter may interpret it as upstream authority loss. No protocol change is proposed on this audit branch.
