# New nominal cohort

No new nominal SITL run has been counted on this evidence branch. The frozen ten-run predecessor cohort is re-evaluated first and remains 0/10 qualification-eligible. Starting a five-run cohort while required lifecycle lineage and independent tracking/motion policy are absent would generate more `NOT_EVALUABLE` runs, not validate a closed contract. This is an explicit evidence gap, not a zero-failure claim.

The required future cohort is at least five same-source, same-PX4-binary, true tracking-off runs after producer transition identity and policy provenance are pinned. All attempts, including non-arming or failed sessions, must remain in its denominator.
