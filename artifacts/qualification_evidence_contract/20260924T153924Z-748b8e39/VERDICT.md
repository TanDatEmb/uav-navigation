# Verdict

`POLICY_PROVENANCE_BLOCKED`

The user confirmed that no unified approved C0 tracking/motion threshold contract exists. Source inspection and the ten-run raw distribution do not establish an independent truth-error allocation or measured-motion acceptance objective. Installing the cohort maxima as a PASS threshold would qualify the same data used to select it and violate the requested no-arbitrary-policy rule. Integrated tracking/motion remain `NOT_EVALUABLE`.

The raw re-evaluation also leaves **140 lifecycle transactions unresolved**, all ten references with lineage mismatch, and 0/10 qualification eligible. The branch improves evidence correctness: the recorder no longer inherits unrelated PVA IDs; the reducer exposes exact evidence outcomes and orphan activation; source-time policies are typed while exact PVA heartbeat and changed-certificate conflict behavior remain distinct. These changes do not close producer identity or create a new policy.

Product `src/` bytes, PX4 binary, safety thresholds, planner, World and Hold behavior are unchanged. No new SITL cohort is claimed. This is **not** `EVIDENCE_CONTRACT_CLOSED`; neither software nor integrated flight qualification is claimed.
