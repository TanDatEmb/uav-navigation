# Focused SITL parity

Pending. Reference is the pinned repaired branch `8ffa14e5`: three consecutive seed-0 `long_featured`, tracking-off sessions completed accepted `[0,1,2,3,4]`; adapter handoff gap min/median/max was `4/16/28` ms. This branch's matched runs and fault scenarios must be measured after an authoritative Release build. Component tests are not substituted for those observations.
