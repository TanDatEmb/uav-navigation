# HG-023 retained validator

`validateRetainedCommand()` is unchanged. Its `navigation_runtime/retained_command_decision` diagnostic reports planning cycle, purpose, exact owner snapshot, callback currentness, world validation, state source/receive freshness, known-free body, time-aligned anchor, backup/suffix usability, final disposition and post-decision availability. The focused analyzer requires purpose `kAfterFailedReplacement`, exact current owner and request, post-decision command available, and a preserved-command disposition. It does not treat classifier output as certification.

The current safety ledger HG-023 requires latest-world/anchor/suffix exact identity and lease validation before retaining a failed replacement. Invalid evidence continues through the existing brake/fail-close policy. No certificate, lease or threshold was changed here.
