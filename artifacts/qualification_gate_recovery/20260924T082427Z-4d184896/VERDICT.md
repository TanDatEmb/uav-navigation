# Verdict — PARTIAL_CAUSAL_ATTRIBUTION

The original A2 fail-closed response was correct at the unchanged 200 ms accepted-state lease. A natural traced pilot reproduced an A2-like 481.677 ms accepted gap, with a 481.825 ms producer publication gap and an independent 481.869 ms `/clock` recorder gap for only 4 ms source progression. The adapter's publish→callback and mutex delays were sub-millisecond at the tail. This narrows the failure to the upstream simulated-clock/sensor path, but the first Gazebo/bridge/host component is not proven. No product or harness timing defect was claimed or patched.

The ten-run true tracking-off cohort used one source SHA and pinned PX4 binary: 8/10 mission COMPLETE, eight consecutive, 2/10 terminal recovery safety stops, zero >100 ms accepted-state gaps, and complete accepted-state trace joins. All ten evaluator assessments remain `NOT_EVALUABLE`: 140 unresolved lifecycle transactions in aggregate, resulting reference lineage mismatch, and absent approved tracking/motion policies. Zero qualification-eligible runs. The two safety stops are not discarded or attributed to state transport.

The branch fixes evidence wiring, exact heartbeat accounting and diagnostic temporal observability only. It does not meet the user's `QUALIFICATION_GATE_RECOVERED` conditions. Phase B World and Phase C PX4 architecture work must remain gated until the upstream causal split, independent terminal recovery finding and evaluator contracts are closed with repeatable evidence. The next work item is a focused bridge/Gazebo scheduling witness alongside an exact terminal-recovery investigation; no threshold tuning is authorized by this cohort.
