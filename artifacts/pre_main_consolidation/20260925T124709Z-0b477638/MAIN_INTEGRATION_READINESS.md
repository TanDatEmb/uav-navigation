# Main integration readiness

Architecture regression and C0-SW evidence plumbing are ready for integration review. World milestone stays frozen/hardened. C0-SW/C0-IFP output is unambiguous. N1 and N2 have causal classifications. Current exact OptimizationFailed seam and Core-pause lease fail-close were exercised. The nominal cohort has 5/5 C0-SW eligible, but only 3/5 missions completed; two safely stopped and one completed trial's runtime verdict failed the BRAKING-inclusive scenario speed assertion. These are explicit pre-beta debt, not resolved.

Fresh authoritative gate passed on candidate source. Current `origin/main` (`287b7b84cf4311e31d52ca04796223cc4efc1bc5`) is an ancestor; candidate diff is 519 files including the approved historical architecture stack. Current campaign itself adds no `src/` changes. No P0, unclassified P1, or unknown safety failure is identified. This supports architecture integration only, not beta. Normal PR CI and required review remain mandatory; do not self-bypass.


Integration checkpoint before the P1 repair: PR #2 was open at `65f33f683ba25956d394e924f1ed53f59bee15b1`; a Codex P1 identified hard-coded branch baselines. The repair now requires an explicit reachable baseline for historical branch-scope checks and removes those checks from the permanent gate. The repair candidate passed a fresh exact-HEAD local `pre_main_gate.py`: 23-package Release build, 96/96 product CTest, 460 Python tests with 1 skip, permanent static guards, ledger, and diff check. The two hosted CI jobs and regression tests have been added; hosted checks have not run before push. Full ROS product CI has no available self-hosted runner and remains `CI_BUILD_ENVIRONMENT_REQUIRED`. The P1 must remain unresolved until the fix is pushed, hosted CI runs, and the exact latest PR head receives a new Codex review.

GitHub has one human collaborator and `main` is not protected. The temporary single-maintainer review procedure is recorded in `GOVERNANCE.md`; it does not imply independent human approval or server-enforced branch protection. No merge has been performed.
