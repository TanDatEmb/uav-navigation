# Lịch sử lỗi trong quá trình tái thiết kế

Tài liệu này ghi nhận lỗi phát hiện trong từng phase refactor và kết quả
validation song song. Đây là nhật ký kỹ thuật, không phải danh sách phiên bản
code. Mỗi mục phải giữ nguyên lệnh kiểm chứng và artifact để có thể truy vết.

## Quy ước xử lý

- **Fix ngay**: lỗi phá vỡ build, contract, ownership, timestamp/frame, hoặc
  safety gate hiện tại; phải sửa trước khi giao build cho validation tiếp theo.
- **Ghi nhận sau refactor**: lỗi không chặn phase hiện tại nhưng không được
  tuyên bố đã hoàn tất; phải đóng trước acceptance cuối.
- **Môi trường**: thiếu dependency/hardware/dataset hoặc hạ tầng runner; không
  được biến thành bypass trong product code.

## Nhật ký

| Thời điểm/phase | Phát hiện và bằng chứng | Phân loại | Xử lý / điều kiện đóng |
|---|---|---|---|
| 2026-08-25, contract và tree | Runtime còn phụ thuộc tên/package cũ, source tree trộn interface, runtime, mapping và planner backend; generated artifact cũ cũng làm nhiễu review. | Fix ngay | Đã đổi tên các package sản phẩm chính, chuyển generated artifact cũ sang `.artifacts/retired-generated-20260825/`, và thêm layout/ADR. Cần tiếp tục quét tên ngoại lai còn lại sau khi native planner boundary hoàn tất. |
| 2026-08-25, planning/execution boundary | Planner backend vẫn giữ trajectory committed và runtime còn gọi trực tiếp các hàm commit/sample; facade planning/execution mới chưa thay thế toàn bộ đường chạy. | Ghi nhận sau refactor | Đã thêm `navigation_planning` contracts và `navigation_execution` committed store/sampler; chưa đóng cho tới khi runtime chỉ nhận candidate immutable và một execution authority duy nhất. |
| 2026-08-25, typed state | State propagated trước đây đi qua raw runtime struct, chưa có epoch, source/receive timestamp và frame contract dùng chung. | Fix ngay | Đã thêm `KinematicState`, `ExecutionStateStore`, kiểm tra body/world frame và reset theo localization epoch; đã kiểm chứng runtime 107/107. Cần giữ coverage khi cắt hẳn raw path. |
| 2026-08-25, PX4 command gate | Command chỉ kiểm tra hình thức; `valid_until` chưa bị từ chối khi hết hạn và provenance mission/request chưa được đối chiếu đầy đủ. | Fix ngay | Đã thêm kiểm tra thời gian hiệu lực, mission/waypoint/request identity và reset odometry theo epoch; contracts 5/5, external mode 78/78. SITL vẫn mở. |
| 2026-08-25, planner state boundary | Runtime còn dựng `PlannerExecutionState` rồi đổi sang `RobotState` vendor, làm trùng contract state và che khuất ownership timestamp/frame/yaw. | Fix ngay | Đã chuyển planner sang nhận `navigation_planning::KinematicState`, tính yaw tại odometry boundary và loại bỏ adapter trung gian; build xanh, CTest planning 1/1 executable (4 cases), backend 2/2 (40 cases), runtime 8/8. |
| 2026-08-25, mapping ownership | Lần build đầu của `MappingActor` dùng kiểu quaternion không tồn tại trong adapter (`rog_map::Quatf`). | Fix ngay | Đổi biên giới chuyển pose sang Eigen quaternion/vector, build `navigation_runtime` xanh và test mapping 57/57, runtime 107/107. |
| 2026-08-25, mapping API | Runtime vẫn lộ kiểu map/vendor trong header và tự decode/giữ cloud; mutable map đã chuyển vào `MappingActor` nhưng input adapter và telemetry chưa tách hết. | Ghi nhận sau refactor | Tiếp tục đưa cloud, outcome và telemetry về `navigation_mapping` product types; không để runtime giữ mutable map handle. |
| 2026-08-27, mapping boundary hardening | Mapping actor nhận cloud mutable, có thể chấp nhận observation/odometry lệch timestamp hoặc lặp thời gian; grid export cũng chưa phân biệt evidence state với `OUT_OF_MAP`/undefined. | Fix ngay | Đổi cloud thành `unique_ptr<const PointCloud>`, kiểm tra timestamp/pose/epoch/sequence trước mutation, cache nearest-offset metadata, dùng `unique_ptr` cho backend và dịch outcome/state fail-closed. Thêm actor contract tests; mapping 2/2 và runtime 7/7 pass. Dataset/SITL/sanitizer vẫn mở. |
| 2026-08-27, world-model state policy | Snapshot, corridor và A* có các so sánh cục bộ; nhánh aggregate của A* còn dùng `UNDEFINED` như trạng thái tạm có thể đi tiếp, làm mất nghĩa fail-closed của state. | Fix ngay | Thêm predicate `isCellTraversable()`/`isStoredCellState()`, dùng policy chung ở snapshot, trajectory validator, corridor và A*; missing neighbour được biểu diễn là `UNKNOWN`. Thêm ma trận policy và invalid-state regression; dataset/SITL parity vẫn mở. |
| 2026-08-25, validation song song | Agent validation đang chạy bounded sim/dataset/replay trên build đã xanh; kết quả chưa có tại thời điểm ghi mục này. | Chờ kết quả | Chỉ ghi PASS sau khi có scenario, command, artifact path, timestamp và verdict; failure safety phải quay lại fix ngay. |
| 2026-08-25, test artifact provenance | `colcon test-result` cộng một XML 85 test cũ trong build cache với XML 4 case hiện tại, tạo tổng số 89 gây hiểu nhầm. | Fix ngay | Chuyển XML stale sang `.artifacts/test-results-cleanup-20260825/`; CTest hiện tại xác nhận đúng 4 case trong 1 executable của `navigation_planning`. |
| 2026-08-25, test environment | Gọi CTest không source ROS/workspace làm fail `ament_cmake_test`, sau đó source ROS nhưng chưa source workspace làm thiếu `libnavigation_contracts__rosidl_generator_c.so`. | Môi trường | Chuẩn hóa lệnh verification thành `source /opt/ros/jazzy/setup.bash && source install/setup.bash && ctest ...`; với đầy đủ overlay, runtime 8/8 executable passed. Không sửa product code để che lỗi môi trường. |
| 2026-08-25, parallel validation checkpoint | Agent Erdos xác nhận trên snapshot `d3fd0e5`: release build 21 packages, focused 480/480, Python 139/139; dataset shadow planning PASS 100 READY/0 EMERGENCY, mapping 1692/1692. | Ghi nhận sau refactor / môi trường | Dataset replay bị giới hạn 180 giây (`IMU 34173/55435`, `LiDAR 1709/2772`) nên report FAIL vì thiếu source count; SITL `sanity_open/positive` không launch do build manifest stale sau source transition. Cần rebuild/manifest mới rồi chạy lại; chưa có E2E/SITL acceptance claim. Artifact: `.artifacts/runtime/dataset-20260825T154858-128557/report.json`, `.artifacts/runtime/external-mode-check-20260825T155240-132169/report.json`. |
| 2026-08-25, adversarial advisor | Lượt cố vấn Copernicus không trả report sau timeout và được đóng để giải phóng tài nguyên; không có kết luận được dùng làm evidence. | Môi trường agent | Khởi tạo lượt cố vấn Poincare mới, bounded/read-only; chỉ tích hợp findings khi có report và exact anchors. |
| 2026-08-25, adversarial architecture review | Advisor Archimedes chỉ ra runtime vẫn gọi trực tiếp backend commit/sample (`navigation_runtime_node.cpp`), mapping header còn lộ map/vendor types, và planner backend còn giữ execution state; đây là ownership risk dù focused tests xanh. | Ghi nhận sau refactor | Đã đóng state input bằng `KinematicState`; tiếp theo phải chuyển hẳn candidate commit/sample sang `navigation_execution`, che vendor types khỏi runtime API, rồi chạy sanitizer/replay/SITL lặp lại. Không coi unit-test green là acceptance. |
| 2026-08-25, release provenance checkpoint | Sau checkpoint docs `8c5e9b0`, build Release authoritative đã cập nhật manifest với `git_head=8c5e9b0`, `git_dirty=false`, source fingerprint khớp và 318 artifact; validation manifest hợp lệ. CTest chính xác: planning 1/1 executable, backend 2/2, runtime 8/8; Python runtime contract 139/139. | Đã xác minh | Giữ checkpoint này làm rollback point. Đây mới là build/unit-contract evidence; chưa thay thế sanitizer, dataset đầy đủ nguồn, SITL lặp lại hoặc hardware acceptance. |
| 2026-08-25, bounded parallel validation | Hai agent mới được giao một lượt dataset/SITL và một lượt phản biện kiến trúc trên `8c5e9b0`, nhưng không trả báo cáo trong thời gian bounded; đã gửi lệnh chốt, đóng cả hai và xác nhận không còn detached worktree. | Môi trường agent | Không sử dụng lượt này làm PASS/FAIL sản phẩm và không thay đổi gate. Dùng báo cáo Erdos trước đó cùng review Archimedes làm evidence đã có; các lượt validation tiếp theo phải giới hạn thời gian và ghi artifact ngay khi có kết quả. |
| 2026-08-25, execution ownership cutover | Runtime còn lấy mẫu trực tiếp từ backend `CmdTraj`, trong khi `navigation_execution` đã có immutable bundle store nhưng chưa là authority thật; product point cũng thiếu role/completion/trajectory-time để truyền nguyên vẹn contract PX4. | Fix ngay | Backend giờ export một `navigation_planning::CandidateBundle`; runtime commit qua `CommittedBundleStore` và command timer chỉ dùng `CommandSampler`. Goal/localization/world/generation được kiểm tra trước khi expose; export/store rejection giữ fail-closed. Build xanh; planning 1/1, execution 2/2, backend 2/2, runtime 8/8. Dataset/SITL/sanitizer vẫn mở. |
| 2026-08-25, product vocabulary hygiene | Test names, runtime diagnostics/comments và một số vendor-boundary comments còn lộ tên tham khảo hoặc nhãn phase (`SUPER`, `P1`, `V0/V2`), làm product source khó đọc và dễ nhầm với release/version line. | Fix ngay | Đổi sang tên product-neutral trong product/test code; giữ nguyên license headers, UPSTREAM/provenance, external vendor package names và PX4 protocol/topic suffixes vì đó là yêu cầu pháp lý/tương thích, không phải code-line version. PX4 external 3/3, PX4 odometry 6/6 và runtime 8/8 CTest passed. |
| 2026-08-25, SITL planner failure | Artifact `.artifacts/runtime/external-mode-check-20260825T163927-167116`: map/state inputs were valid, but `PlanFromRest` returned `FAILED` at `main_minco`; `OptimizationExpTrajInPolytopes` reported a non-finite MINCO objective, `committed_generation=0`, and PX4 received no finite yaw/yaw-rate setpoint. | Fix ngay | Added finite corridor-plane and initialized-duration validation before MINCO in `src/planning/navigation_planning_backend/src/traj_opt/exp_traj_optimizer_s4.cpp`; no threshold or bypass changed. The original SITL remains open until a rerun identifies whether the generated corridor or optimizer arithmetic is the source. |
| 2026-08-25, direct dataset evidence | Clean HEAD `7f84ff9` dataset artifact `.artifacts/runtime/dataset-20260825T163544-166248` reported `FAIL` only for source coverage (`IMU 34700/55435`, `LiDAR 1735/2772`); LIO tracking, mapping accounting, corrected odometry and planner processing had no runtime exceptions. | Môi trường / ghi nhận sau refactor | Không dùng làm acceptance. Report ghi nhận giới hạn thời gian/coverage; cần một replay đủ nguồn sau khi runtime refactor ổn định. |
| 2026-08-25, parallel validation at `c4fdaab` | Dataset agent stopped before launch because SITL held the canonical runtime lock; SITL agent ran once and produced `.artifacts/runtime/external-mode-check-20260825T165529-180787`: `BLOCKED`, `trajectory_received=0`, `pva_command_received=0`, planner `PlanFromRest` failed, PX4 source tree dirty, simulation clock had stale events and a 708 ms callback gap. | Fix ngay / môi trường | Planner failure is a product issue and remains open; dirty PX4 and stale-clock evidence are environmental limitations. Both agents were closed after results; no process or detached worktree remained. |
| 2026-08-26, planner context naming | Internal `ros_interface` directory/class described clock/log/visualization callbacks, not the product ROS contract; it obscured the boundary alongside `navigation_contracts` and PX4 adapters. | Fix ngay | Renamed it to `planner_runtime_context::PlannerRuntimeContext`, removed the unused ROS1 wrapper, renamed the test, and verified planning backend 2/2 plus runtime 8/8. |
| 2026-08-26, empty trajectory telemetry | SITL decision metrics called trajectory accessors while no bundle was committed, producing a huge non-physical midpoint value in logs. | Fix ngay | Metrics now require `committed_generation > 0` and a non-empty trajectory before sampling; runtime 8/8 passed. |
| 2026-08-26, ASan verification | ASan first exposed a test lifetime bug: `get_rmw_qos_profile()` was referenced from a temporary QoS object. The same run also timed out three replay stress assertions because 10,000 IMU samples made sanitized replay exceed the bounded wait; `uav_simulation` was selected for testing without a corresponding product build directory. | Fix ngay | Keep the QoS object alive in the test, size replay history through the named sanitizer-safe stress fixture, and remove the asset-only simulation package from the product test selector. Focused `fast_lio_ros` ASan 12/12 and full product ASan 11 packages passed with no sanitizer report; `build.py check` reports 68 tests, 0 errors/failures/skips. |
| 2026-08-26, parallel validation environment | The first replacement dataset/SITL agents were started before the authoritative Release manifest was rebuilt and could not verify the current source. Two earlier launches also rejected an artifact root whose basename did not satisfy the runner contract. | Môi trường agent | Closed those runs without product claims, rebuilt the Release manifest at `9ac59b5`, and reran with a valid `runtime` artifact root. All agent processes and detached worktrees were closed/cleared before continuing. |
| 2026-08-26, post-build dataset replay | Artifact session `dataset-20260825T180406-248198` on `9ac59b5`: source IMU `55435/55435`, LiDAR `2772/2772`, corrected odometry `2756`, propagated odometry `13774`, runtime drops `0`; LIO, mapping and shadow planning passed. `flight_acceptance=false` by contract, so mission/waypoint/cross-track metrics are unavailable. | Đã xác minh ở mức dataset / ghi nhận sau refactor | Keep as recorded-data health evidence, not closed-loop acceptance. Synthetic shadow teardown takeover, startup `base_link` TF warning and `0.9833x` playback are harness/environment findings; no product bypass was added. Artifact was removed after report and provenance remains in the Release manifest. |
| 2026-08-26, post-build SITL External Mode | Artifact session `external-mode-check-20260825T180314-246638` on `9ac59b5`: startup, External Mode, takeoff and 7 acknowledgements passed; planner committed one bundle and solved in `4.483 ms`, but execution lease became `RECEIVE_STALE` (source age `20 ms`, receive age `514.760 ms`). Runtime fail-closed to `PAUSED_SAFETY_STOP`/PX4 Hold; only waypoint `[0]` completed, mission incomplete. | Fix ngay | Do not relax freshness or safety gates. Investigate scheduling/clock and typed-stream propagation as the root cause, then repeat representative SITL; retain the failure as a safety-stop regression until mission completion and repeated evidence are obtained. Artifact was removed after report; exact session name and metrics are preserved here. |
| 2026-08-26, product test selector audit | Release build included mapping/vendor transitively, but `tools/runtime/build.py test` omitted `rog_map_vendor` and `navigation_mapping`, so their CTest coverage was not part of the default product test report. | Fix ngay | Added both product packages to `PRODUCT_TEST_PACKAGES`; run the full Release selector and `build.py check` before the next checkpoint. |
| 2026-08-26, corridor normalization adversarial review | Initial shared helper compiled and focused tests passed, but advisors found EXP called `SimplifySFC()` before validation, the public helper exposed a vendor matrix type, `noexcept` covered a dynamic allocation, and overflow could mutate input before returning false. | Fix ngay | Moved validation before EXP geometry, made the helper scalar-generic and product-owned, removed `noexcept`, normalized a copy and commit only after finite verification, and expanded scale/overflow/no-mutation tests. Re-run focused build/test before checkpoint. |
| 2026-08-26, corridor test assertion | The first rerun still reported a failure because Eigen `isApprox` returns false for unchanged NaN values; the assertion confused NaN comparison semantics with input mutation. | Fix ngay | Rebuilt the test target after the assertion change and checked finite elements plus `std::isnan` explicitly. `navigation_planning_backend` build and 2/2 CTest executables now pass. |
| 2026-08-26, corridor validation checkpoint | Post-commit verification at `2e916c4`: authoritative Release build completed 21/21 packages; Release product selector completed 13/13 packages; `build.py check` reported 68 tests with 0 errors/failures/skips; dataset catalog check passed; ASan build completed 21/21 packages and ASan selector completed 13/13 packages with 100% test pass in all executed CTest suites, including 12/12 `fast_lio_ros` tests and no sanitizer report. | Đã xác minh ở mức build/test/sanitizer | Checkpoint is stable for the next investigation. This evidence does not close the known `RECEIVE_STALE` SITL safety regression, backup certificate HG-013, or remaining vendor/API cleanup. |
| 2026-08-26, retained-state post-solve review | Both advisors identified that retained/emergency validation used `execution_state` captured before a potentially long solve (`navigation_runtime_node.cpp` around the retained-command branch), while the command publisher used the live execution lease. | Fix ngay | Reloaded `ExecutionStateStore` after solve and required the dual-clock freshness contract before anchor/emergency validation; added a 20 ms source / 514.760 ms receive-age regression assertion. Focused Release build passed; `navigation_execution` 2/2 and `navigation_runtime` 8/8 passed. Transport/executor/worker root cause of SITL `RECEIVE_STALE` remains open. |
| 2026-08-26, propagated-state callback contention | Advisors found propagated odometry shared the default mutually-exclusive subscription group with synchronous cloud/registered-scan decode while the node has two executor threads; this can create a receive-age gap without a stale ROS header. | Fix ngay | Assigned propagated odometry to a dedicated reentrant callback group without changing executor count, QoS or freshness. Release `navigation_runtime` build and 8/8 CTest passed; runtime Python contracts passed 168/168. Dataset agent preflight was correctly blocked after this uncommitted source change and made no product claim; rerun after the checkpoint is required. |
| 2026-08-26, post-callback dataset replay | Artifact `dataset-20260825T184847-296327` on exact HEAD `a429c49d`: full source coverage (`55435/55435` IMU, `2772/2772` LiDAR), corrected odometry `2756`, propagated odometry `13775` at 50 Hz; LIO tracking/transport and mapping accounting passed with zero drops or violations; shadow planner produced 100 ready commands and zero emergency commands. | Đã xác minh ở mức dataset / ghi nhận sau refactor | Runner verdict `PASS`, no runtime failure and no bypass; `flight_acceptance=false` by dataset contract, so mission/waypoint/clearance acceptance remains open. Warnings: synthetic teardown takeover, missing `base_link` TF, deprecated odometry parameter, `publication_skip_count=41660`, and one terminal stale event; distinguish scheduling skips from data loss before acceptance. State cleanup passed and the agent was closed. |
| 2026-08-26, post-callback SITL rerun | Artifact `external-mode-check-20260825T185357-297920` on exact HEAD `a429c49d`: startup, External Mode, takeoff and 10 ACKs passed; planner committed generation `1` in `3.296 ms`; 443/443 commands succeeded, but one trajectory sample failed at the execution boundary and no valid backup remained. Safety stopped at `32.376 s`, PX4 Hold followed in 24 ms; only waypoints `[0,1]` of `[0..4]` were accepted. | Fix ngay | Keep freshness and safety gates unchanged. Simultaneous gaps were `/clock 1734.788 ms`, propagated odometry `1754.745 ms`, corrected odometry `1819.013 ms`, IMU `1739.964 ms`, and LiDAR `1560.102 ms`, indicating simulator/transport/scheduling blackout rather than a stale-threshold issue. Validation contract also reports `mapping conservation equation failed` while runtime diagnostics report `observation_accounting_valid=1`, `violation_count=0`; fix this reporting root cause before acceptance. Agent cleanup passed and the agent was closed. |
| 2026-08-26, execution-state ownership consolidation | Advisors found command/planner state already came from `ExecutionStateStore`, while diagnostics still used `latest_propagated_*`; with a reentrant callback group this could pair a new state with an old sequence. | Fix ngay | Added immutable `ExecutionStateLease{KinematicState, ingress_sequence}`, assigned the sequence only after store acceptance, removed the three runtime shadow fields and unused legacy planner-state header. Execution 2/2, runtime 8/8 CTest and Python contracts 141/141 pass. Producer-owned epoch for propagated `nav_msgs/Odometry` remains open; this phase does not claim to close reset safety. |
| 2026-08-26, mapping lifecycle report false-positive | SITL report `external-mode-check-20260825T185357-297920` merged a planner `DECISION_TRACE` without lifecycle fields with a later world event lacking replacement counters, then treated missing counters as zero; runtime accounting itself was valid (`1`, violations `0`). | Fix ngay | Both diagnostic producers now serialize the canonical `ObservationAccounting::Snapshot`; planner uses those counters instead of duplicate cloud counters. Report selects lifecycle-shaped events, skips decision traces for arithmetic, and reports missing evidence instead of defaulting to zero. Added rejected-input and exact event-order tests; Python contracts 141/141 pass. Rebuild/replay required before final SITL verdict. |
| 2026-08-26, post-lease dataset replay | Artifact `dataset-20260825T192106-320079` on exact HEAD `68879a0`: IMU/LiDAR source coverage `55435/55435` and `2772/2772`; corrected odometry `2756`, propagated `13775` at 50 Hz with max gap `29.804 ms`; LIO tracking/transport passed; mapping received/accepted `2758/2758`, nonmonotonic/discarded pending `2/2`, mapping published `2756`, accounting valid with `0` violations; shadow planning 100 ready/0 emergency. | Đã xác minh ở mức dataset / ghi nhận sau refactor | Runner `PASS`, reasons empty, authoritative Release manifest valid. `flight_acceptance=false` remains a dataset-only contract. One terminal IMU stale event, startup TF warning, deprecated parameter and six planner defaults are recorded; no report conservation false-positive. Agent cleanup passed and agent was closed. Artifact: `.artifacts/runtime/dataset-20260825T192106-320079/report.json`. |
| 2026-08-26, post-lease SITL rerun | Artifact `external-mode-check-20260825T192608-321637` on exact HEAD `68879a0`: startup/External Mode/takeoff passed, but planner `PlanFromRest/main_minco` produced a non-finite objective, no candidate/PVA (`trajectory_publish_count=0`), and only 214 finite PX4 setpoints without yaw-controlled setpoints. Safety stopped at `26.808 s`, PX4 Hold after `204 ms`; only waypoint `[0]` accepted; cross-track p95 `0.7693 m`. | Fix ngay | `BLOCKED`, not acceptance. Mapping `283/283/283/283` accepted/started/updated/published and accounting valid; the lifecycle report contradiction is absent after the schema fix. Keep freshness/safety gates unchanged. Investigate the remaining non-finite MINCO/corridor root and simultaneous IMU/external-odometry arrival gaps (up to `398.735/404.068 ms`), then repeat SITL. Agent cleanup passed and agent was closed. |
| 2026-08-26, MINCO non-finite diagnosis instrumentation | Two advisors independently placed the causal boundary after corridor frontend: the first solve returned `LBFGS_STOP` with velocity violation `1.02482`, then the retry returned `LBFGSERR_INVALID_FUNCVAL (-1012)`. The retry duration parameterization and missing finite checks prevented identifying the first invalid evaluation. | Fix ngay | Added diagnostic-only provenance for the first non-finite evaluation (input, duration, points, MINCO, objective or gradient stage; solver attempt/iteration and duration range), and reject non-finite cost/gradient fail-closed. No gate, threshold, fallback or candidate authorization changed. Focused Release build passed; planning CTest 2/2, runtime CTest 8/8, and Python contracts 147/147 passed. Next step is a replay with these fields before changing retry behavior. |
| 2026-08-26, post-instrumentation parallel validation | Dataset artifact `dataset-20260825T195426-343584` on `77d7cdd`: full IMU/LiDAR coverage, LIO tracking, propagated odometry `49.9996 Hz`, mapping `2756/2756` with zero violations, and shadow planner `100 READY/0 EMER`; planner trace had zero non-finite evaluations. SITL artifact `external-mode-check-20260825T195939-345561` remained `BLOCKED`: retry objective became non-finite at stage `5`, attempt `2`, iteration `46`, duration range `2.601398–4.956079 s`, no candidate/PVA, and a simultaneous clock/transport blackout up to `783.1 ms`. | Fix ngay | Dataset is recorded-data health evidence only (`flight_acceptance=false`). The SITL result confirms the retry objective path is the product blocker; common simulator/transport scheduling remains an independent blocker. Both agents returned, were closed, and cleanup/worktree/process checks were clean. Preserve fail-closed behavior and do not relax freshness or mission gates. |
| 2026-08-26, retry duration parameterization | The retry previously represented a full base-duration floor plus an independent 1% mapped slack, so the free-duration optimization variable was poorly scaled even though the reconstructed total duration was finite. | Fix ngay | Changed the retry to use an additive per-segment reserve and initialize the free duration from the last finite solution; added finite/positive checks for base duration, reserve, offset and tau. Focused Release build passed; planning CTest 2/2, runtime CTest 8/8 and Python contracts 147/147 passed. This is not closed until a fresh exact-HEAD replay confirms stage-5 objective/gradient finiteness and preserves all final gates. |
| 2026-08-26, retry validation and lock orchestration | Dataset `dataset-20260825T202030-362330` on `7cea790` completed `PASS` with full IMU/LiDAR coverage, LIO/mapping health, zero non-finite optimizer evaluations and zero value-mask bits. The parallel SITL agent initially hit the canonical runtime lock held by the dataset runner; after safe cleanup, the SITL rerun produced artifact `external-mode-check-20260825T203002-365806`. | Môi trường / fix ngay | Closed the stale dataset process and verified state `STOPPED`, cleanup `PASS`, no process/temp/worktree. The lock collision is an orchestration issue to prevent in future parallel validation; it is not a product bypass or acceptance result. |
| 2026-08-26, retry no-progress and stale candidate diagnostics | SITL artifact `external-mode-check-20260825T203002-365806` had no non-finite evaluation (`count=0`, `mask=0`) but rejected the candidate at velocity violation `1.024820197132971`; retry reached `1.0248201536822739` yet the `1e-6` deadband declared no progress. The optimizer restored only `x`/duration floor while penalty log/objective/final duration could describe the rejected retry; no bundle or PVA was committed. | Fix ngay | Removed the deadband, accept only finite strict improvement or an already hard-feasible candidate, snapshot/restore full optimizer state on retry rejection/cancellation/corridor failure, and emit final duration only after all hard gates. Parser now retains `commit_decision` separately from `candidate_result`. Focused Release build passed; planning CTest 2/2, runtime CTest 8/8, Python contracts 147/147. Fresh authoritative build and repeated dataset/SITL are required. |
| 2026-08-26, exact-head 906 validation | Dataset artifact `dataset-20260825T205111-385361` on `906be2c` had complete IMU/LiDAR coverage, LIO tracking, mapping `2756/2756` with zero accounting violations and zero non-finite evaluations, but shadow planning emitted `EMER` before READY and execution invalidated the committed sample. SITL artifact `external-mode-check-20260825T205808-387989` had non-finite `0/mask=0`, but `LBFGS_STOP` ended two retries at velocity ratio `1.024216`, no candidate/PVA, safety stop and mission incomplete. | Fix ngay / môi trường | Keep all gates fail-closed. Classify the planner stopping criterion as the product fix; classify the canonical lock collision that blocked the parallel SITL attempt as runner orchestration. Both artifacts ended `STOPPED/cleanup=PASS`; agents and processes were closed. |
| 2026-08-26, feasibility retry stopping mode | The retry used the nominal L-BFGS cost-plateau rule (`past=3`, `delta`) even while the hard dynamic residual remained above one; disabling that rule without a bound would risk unbounded latency. | Fix ngay | Added explicit feasibility-retry mode (`past=0`) with positive configured `feasibility_retry_max_iterations`; iteration-limit candidates are still checked by corridor, V/A/J, vertical-guide and flatness gates, and the raw solver result remains diagnostic. Added config coverage; focused and full Release build/test/check passed. Fresh repeated dataset/SITL evidence is required before closure. |
| 2026-08-26, exact 363 validation | SITL artifact `external-mode-check-20260825T211927-408669` remained `BLOCKED`: raw retry return `-1009` (maximum line search), velocity ratio `1.02482`, no candidate/PVA/commit and safety stop. Dataset artifact `dataset-20260825T212458-412383` had full source/LIO/mapping health and zero non-finite evaluations, but shadow planning emitted EMER before READY. | Fix ngay / môi trường | Keep all hard gates fail-closed. Treat the solver/parameterization failure as product work; treat the canonical one-run lock collision as orchestration only. Both sessions ended `STOPPED/cleanup=PASS`; agents, process and worktree cleanup were verified. |
| 2026-08-26, retry duration lower-bound invariant | Review found `minimum_time_floor = base * (reserve - 1)` was only the additive reserve, so L-BFGS could shrink total segment duration below the nominal candidate; a second retry could also re-anchor from mutated durations. | Fix ngay | Working-tree patch renames the EXP state to `duration_lower_bound`, anchors it once to nominal durations, maps a separate positive `free_duration_seed_s`, rejects size mismatch fail-closed and emits lower-bound/seed telemetry. Duration property tests, planning 2/2, runtime 8/8 and Python 147/147 pass. Exact-head replay, full Release checkpoint and repeated dataset/SITL remain required; this patch does not claim to close `-1009`. |
| 2026-08-26, c41f3eb dataset replay | Artifact `dataset-20260825T214950-436370` replayed only 35.06% of source IMU/LiDAR within the bounded run; propagated odometry remained 49.998 Hz, but mapping had one pending replacement/accounting violation and shadow planning produced no READY/commit after a flatness hard-gate rejection with non-finite thrust. | Ghi nhận sau refactor / fix ngay | Do not accept incomplete source coverage. Preserve the flatness fail-closed result and reproduce on a complete replay; investigate whether the non-finite thrust is a planner input/data-window effect or a product numerical defect. No gate or timeout relaxation. Agent/session cleanup `STOPPED/cleanup=PASS`. |
| 2026-08-26, c41f3eb SITL preflight block | Artifact `external-mode-check-20260825T214922-434888` entered External Mode but PX4 refused arm (`pre_flight_checks=false`, `ekf2 missing data`, unstable heading); no planner cycle or duration telemetry was observed. IMU/external-odometry gaps reached 391.78/397.91 ms. | Môi trường / ghi nhận sau refactor | Không dùng làm evidence cho LBFGS/duration. Giữ nguyên preflight/freshness fail-closed; xử lý simulator/estimator startup và chạy lại trước khi đánh giá planner. Agent/process/worktree cleanup đã PASS. |
| 2026-08-26, shared time utility boundary | Runtime had private `stampNanoseconds` and `steadyNowNanoseconds` copies, while PX4 kept a separate nanosecond-to-microsecond implementation. This duplicated the ROS/PX4/simulation-vs-real time boundary and made naming/semantics harder to audit. | Fix ngay | Added shared `navigation_common` steady-clock and nanosecond-to-microsecond helpers; removed runtime duplicate timestamp/steady helpers and routed PX4 `TimestampConverter` through the common conversion. Preserved optional validation and fail-closed sentinel behavior; no freshness gate or clock-domain assumption changed. Focused/full Release verification and parallel dataset/SITL replay are required before closure. |
| 2026-08-26, committed candidate before sample-validity boundary | Exact dataset artifact `.artifacts/runtime/dataset-20260825T222025-461002` had complete source/LIO/mapping health and a committed generation `1`, but the candidate start was about `10.617 ms` in the future. `CommandSampler` returned no point before `valid_from_ns`; runtime treated that expected activation lead as a terminal planner failure and emitted one `REJECTED/EMER` before any READY command. | Fix ngay | Added explicit `awaiting_activation` state to `SampleResult`; runtime now retains the immutable committed bundle and defers that publication tick. Candidate pre-start evaluation, trajectory-time semantics, expiry, evaluator failure and all safety gates remain fail-closed. Added sampler regression for before-boundary retention, boundary sampling and expiry. Replay must prove READY without EMER and preserve continuity; no threshold or hidden bypass. |
| 2026-08-26, sampler regression caught moved-bundle dereference | The first implementation evaluated `bundle->sample()` in the same braced return expression after moving `bundle`, and `test_committed_bundle_store` terminated with SIGSEGV on the second test. | Fix ngay | Evaluate the point into a local object before moving the immutable bundle into `SampleResult`; rebuild and rerun the focused test before any replay. No product run was accepted from the faulty build. |
| 2026-08-26, activation-boundary exact-head validation | On `a791576`, complete dataset replay `dataset-20260825T223901-478552` passed recorded-data health: LIO tracking, mapping `2756/2756` with zero accounting violations, propagated odometry near `50 Hz`, planner `50 READY/0 EMER` on generation `1`, and no runtime failure. The report remains `flight_acceptance=false` because the dataset workflow has no PX4/mission evidence; diagnostics also record `138` duplicate and `1` timestamp-regression event. | Đã xác minh ở mức dataset / ghi nhận sau refactor | Keep this as recorded-data evidence only; investigate diagnostic duplicate/regression accounting and repeat representative runs. Do not infer full-flight acceptance or change gates. Synthetic `OPERATOR_TAKEOVER` teardown is harness behavior. Agent and runner cleanup passed. |
| 2026-08-26, activation-boundary parallel SITL | The SITL agent could not create a session because the dataset runner held the canonical `.build-runtime.lock`; it produced no PX4/planner/odometry evidence. | Môi trường | Do not classify as product PASS/FAIL. Preserve fail-closed lock orchestration, run SITL after the dataset lock is clear, and keep the lock cleanup check in the validation handoff. Agent closed; stale lock files were removed only after their recorded PIDs were confirmed dead. |
| 2026-08-26, duplicate dynamic-limit contract | `navigation_planning` defined both `DynamicsLimits` in `planning_request.hpp` and `DynamicLimits` in `planning_limits.hpp`; runtime mission parsing also included the backend planner header merely to return the aliased type. | Fix ngay | Kept one product-owned `navigation_planning::DynamicLimits` with one `valid()` contract, removed the duplicate spelling, and made `mission_dynamics.hpp` depend only on the product planning contract. No optimizer or safety behavior changed. |
| 2026-08-26, missing package export contract | `navigation_mapping` and `navigation_runtime` installed/exported targets incompletely: target export files and include-directory exports were missing, and mapping did not propagate its direct world-model/message interface. | Fix ngay | Added explicit `install(EXPORT ...)`, include exports, and direct mapping interface dependencies. Clean installed-consumer build is required before treating packaging as closed; no runtime behavior or safety gate changed. |
| 2026-08-26, installed-consumer harness assertion | The first temporary installed-consumer probe called `Snapshot::valid()` on `navigation_mapping::ObservationAccounting::Snapshot`, but that product snapshot intentionally has no such method; the probe failed at compile time. | Fix ngay | Removed the invalid harness assertion while retaining the header include and product `DynamicLimits::valid()` check. The clean installed consumer then configured and linked successfully through the exported mapping/planning/runtime targets; no product source behavior changed. |
| 2026-08-26, exact-head SITL validation after contract checkpoint | Artifact `external-mode-check-20260825T225925-499882` on `1a4b3f8` entered External Mode and armed with preflight passing, but produced no planner PVA/trajectory (`exp_final_normalized_dynamic_violation=1.008735`, final retry return `-1008`, no valid candidate), then fail-closed to `PAUSED_SAFETY_STOP` at `28.52 s`; PX4 Hold handover was observed after `148 ms`, only waypoint `[0]` of `[0..4]` was accepted, cross-track p95 `0.969 m`. | Fix ngay / môi trường | Keep all gates unchanged. The planner feasibility failure and simulator/transport blackout are separate: corrected odometry maximum arrival gap `1624.7 ms`, external odometry was below rate contract with active callback/arrival stalls. Investigate both roots before claiming SITL acceptance. Agent/process/worktree cleanup was completed; the parallel dataset agent was correctly blocked by the SITL lock and produced no artifact. |
| 2026-08-26, runtime planner policy coupled to backend codes | `planner_fsm.hpp` included `navigation_planning_backend/planner.hpp` only to classify legacy integer result constants, allowing backend vocabulary to leak into a runtime policy header. | Fix ngay | Added product-owned `navigation_planning::PlannerStatus`, translated backend codes once in `navigation_runtime_node.cpp`, and moved FSM tests to the product enum. Planner ownership and implementation header coupling remain open for the PImpl phase. |
| 2026-08-26, planner implementation leaked through runtime public header | `navigation_runtime_node.hpp` included `planner_runtime_context` and `navigation_planning_backend/planner.hpp`, and stored the backend smart-pointer alias in an installed product header. This made any consumer of the runtime node transitively depend on planner implementation headers. | Fix ngay | Replaced the includes with forward declarations, changed the private context/planner ownership to standard `shared_ptr`/`unique_ptr`, and made construction explicit in the `.cpp`. `navigation_runtime` build passed, CTest passed `8/8`, and a clean installed consumer configured and linked through product targets. The runtime `.cpp` still directly calls backend APIs; complete facade and backend include allowlist remain open. |
| 2026-08-26, planner-status checkpoint validation | Dataset artifact `.artifacts/runtime/dataset-20260825T231828-526332/report.json` on `b3c94b6` passed recorded-data health with full IMU/LiDAR coverage, LIO/mapping health, zero accounting violations, propagated odometry near `50 Hz`, and shadow planning `51 READY/0 EMER`; `flight_acceptance=false` remained explicit. Parallel SITL was `BLOCKED_ENVIRONMENT` before launch because the dataset runner held `.build-runtime.lock`, so no PX4/planner/mission evidence was produced. | Môi trường / ghi nhận sau refactor | Không dùng dataset PASS làm E2E acceptance và không dùng SITL BLOCKED làm planner verdict. Both agents were closed; owner PID was verified dead; stale lock removed; one worktree and no runtime process remained. Re-run SITL only after a stable boundary checkpoint. |
| 2026-08-26, planner facade boundary | Runtime still consumed backend trajectory/status/certificate types directly after the public-header isolation checkpoint; diagnostics also reset timing state when the watchdog read them, and the product snapshot dropped backup role and pinned-world provenance. | Fix ngay | Added `PlannerFacade` with PImpl, product-only snapshot/status/diagnostic contracts, role/finished preservation, explicit pinned/validated world certificate, non-destructive timing snapshot, and stage/point watchdog accessors. Runtime no longer names backend trajectory/result types. Facade test, planning 1/1, backend 3/3 and runtime 8/8 pass; clean install exposes only `planner_facade.hpp`. |
| 2026-08-26, facade compile-surface regression | Removing backend compile definitions from PUBLIC propagation exposed runtime/backend tests that included private implementation headers without declaring their own test-only ROS build contract. | Fix ngay | Added `USE_ROS2` and `ORIGIN_AT_CORNER` only to the affected internal tests; product targets remain free of those private definitions. Rebuild and focused tests pass. This is a test-boundary correction, not a product compatibility dependency. |
| 2026-08-26, runtime test leaked planner implementation | `test_world_snapshot_store.cpp` pulled `navigation_planning_backend/planner.hpp`, `CmdTraj`, vendor trajectories and backend math into a runtime package test, so the installed-header fence could not be evaluated honestly. | Fix ngay | Replaced the test fixture's command model with a small product-level atomic command probe that checks the same publication/authorization/snapshot invariants. The runtime test remains focused on `WorldSnapshotStore` and no longer requires planner implementation headers. |
| 2026-08-26, clean install probe | The first direct public-header probe omitted the underlay include directory for `navigation_world_model`, producing a harness include error; it was not a product failure. | Môi trường | Added the actual dependency include path to the probe, then verified the C++20 public facade header with `-Wall -Wextra -Werror`, checked that no private planner header exists in the fresh install, and linked a clean external consumer successfully. |
| 2026-08-26, facade checkpoint dataset validation | Exact-head dataset artifact `.artifacts/runtime/dataset-20260826T001126-569319/report.json` had complete IMU/LiDAR coverage, LIO tracking `1.0`, propagated odometry near `50 Hz`, mapping `2756/2756` with zero accounting violations, and shadow planning `98 READY/0 EMER` across generations `1..9`; `flight_acceptance=false` and `experimental_bypasses={}` remained explicit. | Đã xác minh ở mức dataset / ghi nhận sau refactor | Recorded-data health PASS only; no PX4/mission acceptance claim. The runner teardown initially stayed `STARTING` after data completion, then was shut down safely; final `state=STOPPED/cleanup=PASS`, agents/processes/worktree/lock cleanup verified. Artifact retained for traceability. |
| 2026-08-26, facade checkpoint SITL validation | The parallel SITL agent verified exact HEAD `937c8ef` and the Release manifest but could not launch because the canonical `.build-runtime.lock` was held by the live dataset runner PID `569319`; no SITL report or PX4/planner/odometry evidence existed. | Môi trường | Classified `BLOCKED_ENVIRONMENT`, not a planner verdict. Agent was closed immediately; its attempt created no child process. Re-run SITL only after a clean lock/preflight window; do not relax orchestration or safety gates. |
| 2026-08-26, parallel runtime lock starvation | The dataset and SITL validation agents were both started, but `run_dataset()` held the canonical `.build-runtime.lock` exclusively for the entire replay. `run_sim()` also required an exclusive lock, so SITL was rejected before session creation and produced no PX4 evidence. | Fix ngay / môi trường | Changed the contract to exclusive lock for build/test and shared lock for read-only runtime sessions; added separate dataset/SITL session locks, workflow-aware active-session checks, and ROS domain `43` for dataset versus `42` for SITL. Added lock/isolation regression tests and CLI/Make support for `DATASET_ROS_DOMAIN_ID`. No safety gate, timeout, fallback, or product acceptance rule changed. Next validation must prove both agents reach session creation with no cross-talk; dataset remains non-flight evidence. |
| 2026-08-26, unexplained guide-time contraction | The guide branch initialized segment times from the physical guide allocation, then `setupProblemAndCheck()` multiplied them by `0.8` in both implementation and stale inline paths. Exact SITL evidence showed the first candidate at normalized velocity violation `1.024787`, followed by MINCO line-search exhaustion and fail-closed planner stop. | Fix ngay | Removed the contraction from both paths and preserved the guide allocation before hard-gated optimization. No dynamic limit, retry budget, freshness gate, fallback, or acceptance rule changed. Release build, full selected CTest, and runtime Python contracts `173/173` passed. Exact full dataset artifact `dataset-20260826T034428-4` passed with complete source coverage and zero accounting violations. Fresh SITL is still required. |
| 2026-08-26, planner phase timing observation after seed fix | The clean replay after removing the contraction measured planner `planning_latency` p95 `2.343 ms` and `exp_opt` mean `641 us`, versus `1.389/1.410 ms` and `472/380 us` in two earlier clean baselines. Mapping improved in the same run (callback p95 `9.246 ms` versus `11.875/12.505 ms`), while end-to-end replay was statistically close (`0.9824x` versus `0.9833x/0.9851x`). | Ghi nhận sau refactor / tối ưu sau | Treat this as a real phase-level performance signal, not as proof of whole-replay regression. Do not restore the magic contraction or tune a hard gate from one run. Repeat identical A/B runs with isolated CPU/runtime conditions and correlate planner diagnostics with `Observed duration over simulation time`, wall duration, and scheduling gaps before any algorithmic optimization. |
| 2026-08-26, stale planner timing carried across non-executed cycles | Planner diagnostics kept the last `exp_frontend/exp_opt/backup_*` values in setup-only cycles while `exp_diagnostics_valid=false`; report collection counted those values as fresh optimizer executions. This inflated `exp_opt` from the valid-solve mean around `775 us` to the unfiltered `641 us` across 20 mixed records and obscured the valid sample count. | Fix ngay / observability | Centralized the execution-timing validity rule: once the producer exposes `exp_diagnostics_valid`, execution-specific timing is counted only when true; missing legacy flags remain compatible, and planner total/scheduling fields are unaffected. Added a regression test. No planner behavior, safety gate, timeout, fallback, or acceptance rule changed. Re-render and rerun the dataset are required. |
| 2026-08-26, post-rebuild dataset sidecar finalization | After the Release install was rebuilt, sidecar artifact `dataset-20260826T040211-4` captured runtime samples and valid build provenance but remained `STARTING` without `report.json` after the agent stopped responding. It is not a complete replay result. | Môi trường | Closed the sidecar, preserved the partial artifact for traceability, and classified it as invalid for performance or health claims. Process/lock cleanup was checked; a fresh dataset replay is required after the telemetry collector fix. |
| 2026-08-26, collector-corrected replay pair | Clean exact-install replays `dataset-20260826T041003-4` and `dataset-20260826T041557-4` both passed with full source coverage, LIO/mapping health, zero experimental bypasses, and planner timing reduced to 9 valid solves per run. Playback achieved `0.985925x` and `0.986787x`; valid planner solve p95 was `1.976/1.379 ms`, and `exp_opt` mean `742/747 us`. | Đã xác minh ở mức dataset / ghi nhận sau refactor | Whole-replay throughput is not worse than baselines `0.983289x/0.985057x`. The optimizer phase remains a follow-up characterization (`742–747 us` versus valid baseline `603–638 us`), not an acceptance failure and not a reason to restore `0.8` or relax a gate. Dataset remains non-flight evidence; SITL is still blocked by the sandbox UDP restriction. |
| 2026-08-26, duplicate EXP implementation audit | The optimizer header contained inactive `processCorridorWithGuideTraj2()`/`setupProblemAndCheck2()` clones and an unreachable default-initialization branch after a fail-closed throw. The duplicate path had already diverged from the active guide matching/overlap checks and created a second apparent implementation. | Fix ngay / architecture | Removed the inactive clones and orphan helpers, retained the single active guide path and all gates, and updated the port manifest hashes plus provenance note. Required closure evidence is a clean build/test, direct seed-duration regression, and parity audit when the pinned upstream checkout is available. |
| 2026-08-26, public package export leakage | `navigation_planning_backend` exported implementation/vendor link dependencies as if they were part of the facade contract; `navigation_execution` did not express its planning/world-model interface dependencies directly and exported a test-only contracts dependency. | Fix ngay / architecture | Changed backend link visibility and made execution interface links explicit; moved `navigation_contracts` to test-only. Required closure evidence is a fresh-prefix installed-header inventory and strict external consumer compile. |
| 2026-08-26, direct EXP seed regression | Removing the duplicate path needed a behavior-preserving invariant, not only a source grep: the active guide overload must retain the guide terminal time as its initial duration seed. | Đã xác minh ở mức unit / ghi nhận sau refactor | Added `test_exp_optimizer_seed`; a convex-box solve passed with finite trajectory and `initial_duration_s == guide_t.back()`. Full selected verification passed 69 C++ tests and 173 Python tests. SITL remains separate. |
| 2026-08-26, fresh export boundary check | The accumulated install still contained stale `super_core` headers, so it could not prove the new public boundary. | Fix ngay / architecture | Built backend into an empty temporary prefix; only `planner_facade.hpp` was installed and an external C++20 facade include compiled. A fresh execution-only build was correctly rejected by missing dependency package hooks and is still open; no stale install was deleted. |
| 2026-08-26, A* liveness and heap ordering | A* used simulation time for timeout decisions while `/clock` can stall, fallback escape searches were outside the shared stage budget, and mutating a queued node's score through a pointer did not implement decrease-key. | Fix ngay | Added one absolute stage budget, steady-clock cancellation, zero-budget fail-fast semantics, immutable open-set entries, and stale-entry discard. Focused C++20 build and `test_planner_runtime_context` pass 3/3. Repeated frozen-clock/detour/no-path dataset and SITL evidence remains open. |
| 2026-08-26, CIRI tangent numerical degeneracy | `pass_point - pass_point` had been used to detect a zero direction; collinear inputs could still normalize a zero cross product or take a negative tangent radicand, producing NaN planes and downstream planning failure. | Đã xác minh ở mức unit / ghi nhận sau refactor | Corrected the direction contract, added scale-aware finite/inside/tangent checks, deterministic least-aligned-axis fallback, and fail-closed propagation to corridor decomposition. Added a narrow-branch regression proving a non-collinear seed is not artificially perturbed; `test_trajectory` 35/35 and the backend CTest suite 5/5 pass. Full corridor/dataset/SITL characterization remains open. |
| 2026-08-26, repeated YAML parsing and mutable lookup | The shared loader called `YAML::LoadFile` for every parameter and reparsed nested paths, adding startup I/O and making lookup ownership unclear. The first cache attempt also exposed a mutable-node traversal regression: later nested keys became defaults. | Fix ngay | Cache one parsed document, traverse through const nodes plus `reset`, and add required/nested loader tests. `test_rog_map_vendor` passes 14/14 after the regression test caught and fixed the first implementation. Strict schema/range normalization remains a later phase. |
| 2026-08-26, stale solve liveness | `Planner::authorizeAndCommit()` rejected a candidate before latest-world swept validation whenever the map revision differed from the solve-pinned identity; at mapper rates near 10–20 Hz this can starve non-trivial solves. | Fix ngay | Removed only the pre-validation identity rejection. Latest immutable geometry remains authoritative and `commitIfCurrent()` still rejects a publication racing the final swap. Focused build is pending; add stale-pinned/latest-safe and latest-obstacle tests before closing. |
| 2026-08-26, chained comparison in yaw seed timing | `pos_traj_dur > 0.5 > 0` relied on C++ boolean chaining instead of expressing the intended duration clamp; the positive-duration result happened to match, but the code obscured units and triggered a compiler warning. | Fix ngay / clarity | Replaced it with `std::min(0.5, pos_traj_dur)` and added the standard algorithm include. No yaw limit or trajectory gate changed. |
| 2026-08-26, route-reference weight coupled to corridor penalty | EXP derived a vertical quality weight from the largest feasibility penalty, making the route term inherit `5e8`; the same phase also allowed the corridor certificate to rely on the objective penalty log. | Fix ngay | Replaced the derived term with explicit lateral/vertical route-reference weights and deadbands, removed retry scaling of route quality, and made the sampled corridor certificate recompute plane violation independently of the objective. Malformed reference dimensions now fail closed. Post-correction focused build and tests are green; objective-conditioning A/B and repeated dataset/SITL evidence remain required. |
| 2026-08-26, backup safety predicate weaker than atomic transaction | Backup visibility and validation had been able to treat UNKNOWN as traversable, and committed-trajectory revalidation reconstructed only position/yaw, losing BACKUP role intervals. | Fix ngay | BACKUP visibility and swept validation now require KNOWN_FREE; UNKNOWN is mission-controlled only for MAIN, main-only candidates are rejected when that policy is active, role schedules must be finite/continuous/complete, and facade revalidation copies roles. `test_trajectory` 34/34 and `test_planner_facade` 1/1 pass; runtime revalidation and repeated replay/SITL remain open. |
| 2026-08-26, stale certified bundle survived a newer world publication | `CommittedBundleStore::publishWorldIdentity()` replaced the current identity but retained the previously committed bundle, so command sampling could continue a trajectory certified on an older map revision. Both advisors independently classified this as P0 at the mapping-to-execution boundary. | Fix ngay | World publication now clears the committed bundle whenever a strictly newer identity is accepted; an old commit token remains rejected and only a new candidate matching the new identity can commit. `test_committed_bundle_store` now covers the invalidation and replacement path. Runtime command-gap, dataset and repeated SITL distributions remain open; no flight-safety claim is made. |
| 2026-08-26, backup corridor gate coupled safety to penalty weight | Backup authorization used a fixed `0.2` threshold and `penna_pos * 0.05` around `penalty_log(1)`. The position log was also not accumulated across planes, so the gate could miss a geometric excursion and could be disabled by optimizer configuration. | Fix ngay | Added an independent normalized-plane violation evaluator over the output trajectory, compared directly with `corridor_plane_tolerance_m`; penalty logs remain diagnostic. The config test verifies invariance to plane scaling. Continuous polynomial extrema, dataset and repeated SITL evidence remain open. |
| 2026-08-26, solve failures collapsed into backup failure | `classifySolveFailure()` returned `PLANNER_BACKUP_FAILED` for every non-timeout/non-cancel path, including EXP generation and candidate/world rejection. | Fix ngay | Added `PLANNER_EXP_FAILED` and `PLANNER_CANDIDATE_REJECTED` with caller-provided fallback while preserving timeout/cancel precedence. Existing command remains unchanged on rejection; post-change backend build and `test_trajectory` 34/34 pass. |
| 2026-08-26, runtime lifecycle test log path | The shutdown integration test initialized ROS logging under `/home/letandat/.ros/log`; the managed sandbox rejected that write and the failed context setup surfaced as SIGSEGV. No product callback or shutdown path had run. | Môi trường | The test now creates a writable `/tmp/uav-navigation-test-ros-logs` directory before ROS initialization. With the corrected map/backend install, the shutdown contract passes; UDP discovery still emits sandbox `Operation not permitted` diagnostics and is not counted as SITL evidence. |
| 2026-08-26, stale installed map binary during runtime relink | The runtime package linked the accumulated installed map library while the source snapshot/live ray contract had changed, producing a false parity failure in `VirtualPlaneClassificationAndRayBoundariesMatchBackend`. | Fix ngay / provenance | Rebuilt and installed the map vendor target, relinked the planning backend, rebuilt runtime, and reran the mapping suite: 7/7 runtime mapping tests and 16/16 map-vendor tests pass. Future runtime verification must source the matching workspace overlay and refresh dependency installs before interpreting parity failures. |
| 2026-08-26, hot-retarget candidate provenance | Backend export accepted the currently supplied goal/localization/request fields without proving that the committed trajectory was planned for that identity; a retained old bundle could therefore be relabeled by runtime. | Fix ngay | Persisted command identity with the backend candidate, set it before each solve, reject export on mismatch, and made `CommandSampler` reject a retained bundle from the active goal epoch. `test_planner_facade` 1/1, `test_committed_bundle_store` 5/5 and runtime relink passed. Full runtime replay/SITL evidence remains open. |
| 2026-08-26, dual planner/execution commit authority | Runtime readiness was inferred from the backend `CmdTraj` generation even though `navigation_execution` also owned the exposed immutable bundle. A backend commit could therefore be mistaken for an execution commit. | Fix ngay | Planner now stages the certified candidate, runtime commits it exactly once through `CommittedBundleStore`, then ACKs the planner history. Readiness checks staged-candidate presence; ACK failure clears exposure. Focused backend/runtime/execution tests and relink passed; integration replay/SITL remain open. |
| 2026-08-26, dead and misleading parameters | `detailed_log_en`, `backup_traj_en`, `mpc_horizon`, `yaw_mode`, `input_pair_max_skew_s`, raw bridge deployment flags, and YAML-only `energy_cost_type/scale_factor` did not control product behavior; `penna_margin` actually changed only flatness gate slack. A* also duplicated world geometry through `map_voxel_num`. | Fix ngay / clarity | Removed dead declarations/keys, removed raw bridge flags in favor of derived diagnostics, and removed `astar/map_voxel_num`; the immutable world snapshot now supplies the search-window dimensions and sparse A* state allocates only touched nodes. The intermediate flatness slack name was later removed because a hard physical gate must not have a tunable widening factor. |
| 2026-08-26, hardware clock-domain mapping gap | The external bridge initially treated `use_sim_time=false` as unresolved because it did not distinguish a ROS/system-time source from arbitrary sensor-time input. That obscured the actual PX4 contract: the uXRCE-DDS session owns the ROS-to-PX4 offset; a local bridge offset would risk applying it twice. | Fix ngay / safety contract | Added a typed mapping mode: `simulation_time` requires `use_sim_time=true`; `ros_time`/`system_time` require `false`; mismatched and `sensor_time` input remain fail-closed. The bridge sends ROS-clock timestamps and applies no local offset. PX4 uXRCE-DDS supplies its session offset during serialization/deserialization after synchronization converges; `TimesyncStatus` remains diagnostic evidence, not a duplicate product authority. The bridge rebuild and timestamp CTest are 6/6 green. Repeated SITL, realtime recorded-data and target-hardware evidence remain required; no flight-ready claim is made. |
| 2026-08-26, duplicate command acceptance tuning | PX4 external mode independently loaded `command_anchor_max_error_m` and `command_tracking_lag_s`, while runtime separately loaded the same anchor concept. The lag parameter enlarged forward acceptance based on commanded speed, so the acceptance region was not one geometric contract. | Fix ngay / safety contract | Removed both node-local behavior parameters and both YAML keys. Runtime and PX4 now use the named product constant `navigation_contracts::kCommandAnchorErrorLimitM`; forward, reverse and lateral error use one finite geometric limit. Tracking-envelope direct/CTest 8/8 and runtime CTest 7/7 pass after sourcing ROS/workspace overlays. SITL/recorded-data distributions remain required before changing the constant. |
| 2026-08-26, user-facing duration unit mismatch | Estimator and PX4 profile keys exposed raw `_ns`/`_ms` values while core contracts consumed integer nanoseconds, making a valid-looking YAML value easy to misread or scale incorrectly. | Fix ngay / clarity | Renamed profile durations to `_s`, added one checked `navigation_common::secondsToNanoseconds()` conversion at the loader boundary, and retained ns only in internal timestamp/config fields. FAST-LIO CTest 12/12, PX4 bridge CTest 6/6 and parameter-loader 20/20 passed; sandbox UDP diagnostics were environment noise, not test failures. |
| 2026-08-26, aggregate planner radius duplicated component budgets | `planner/robot_r` was loaded independently even though runtime safety geometry already defined vehicle, tracking, localization, mapping and planning-margin owners; the validator only detected drift after loading. | Fix ngay / ownership | Removed the YAML key and derive `robot_r` directly from the five component budgets before braking/corridor calculations. This preserves the current 0.80 m result while eliminating one behavior knob; focused config/trajectory verification and replay remain required. |
| 2026-08-26, mission parameters with no consumer | Mission YAML/parser carried `planning.replan_rate_hz` and `control.pass_through_lookahead_m`, but no runtime or mission-controller path consumed either value. They looked like behavior controls while silently doing nothing. | Fix ngay / clarity | Removed both fields from the mission struct, strict parser, tests and product mission profiles. Runtime owns planner cadence; mission controller owns waypoint acceptance. Mission parser tests and a fresh full profile validation are required before checkpoint. |
| 2026-08-26, legacy navigation parameter remained an implicit second topic owner | `navigation_runtime.odometry_topic` was still declared as a compatibility fallback even though all product profiles had migrated to `propagated_odometry_topic`. Keeping it live allowed an old parameter file to silently select a different input topic. | Fix ngay / ownership | Removed the deprecated declaration and fallback; the runtime now has one propagated-odometry topic parameter with one default. Product config review and runtime build remain required before checkpoint. |
| 2026-08-26, A* workspace duplicated map geometry and reserved a large pointer table | `astar/map_voxel_num` duplicated the world-model dimensions and caused an eager table of roughly 25 million pointer slots before the first search. This was a strong source-level candidate for RSS/cache pressure and worse observed wall time. | Đã xác minh ở mức build/unit / ghi nhận sau refactor | Removed the YAML geometry parameter, derive asymmetric local extents from the pinned `WorldGeometry`, and allocate only touched search nodes in a sparse workspace. Rebuilt `navigation_planning_backend`; CTest passed 5/5 after sourcing the ROS/workspace overlays. Repeated RSS, planner-latency, dataset and SITL distributions remain open. |
| 2026-08-27, CTest launcher initially lacked the ROS Python module | The first post-change CTest invocation failed all five tests before executing their binaries because `/usr/bin/python3` could not import `ament_cmake_test`; the failure was in the launcher environment, not a test assertion. | Môi trường / đã phân loại | Re-sourced `/opt/ros/jazzy/setup.bash` and the workspace overlay, confirmed the module path, and reran the same CTest command: 5/5 passed. This remains an environment prerequisite for future evidence; no product gate was relaxed. |
| 2026-08-27, YAML optional type mismatch | Shared loader replaced a present malformed optional value with its default, so a typo could silently change planner/mapping/timing behavior. | Fix ngay | Present invalid scalar/sequence and explicit null now fail closed; only an absent optional key uses its default. `rog_map_vendor` build and CTest 1/1 passed, including the new regression cases. |
| 2026-08-27, inflated snapshot layer bounds | Snapshot traversal checked only the base layout; an inflated query outside its own storage could be treated as free under `kAllowUnknown`. | Fix ngay | Added selected-layer containment while keeping the evidence window authoritative, so inflated halo storage cannot extend the product world. Added shifted-layer regression; `navigation_runtime` build and CTest 7/7 passed on rerun. |
| 2026-08-27, backup certificate contradicted mapper profile | Product planner allowed UNKNOWN for exploratory MAIN while raycasting was disabled, but BACKUP required real `KNOWN_FREE` evidence. Source review and smoke-test semantics showed endpoint-only updates leave the sensor-to-hit segment UNKNOWN. | Fix ngay / validation mở | Enabled sensor-origin raycasting in the product planner profile; BACKUP validator remains `KNOWN_FREE` and inflated-occupancy fail-closed. Mapping/export p50/p95/p99 on dense replay and repeated SITL/hardware evidence are still mandatory because this may increase callback cost. |
| 2026-08-27, command exposure after world invalidation | A sampler could hold an old immutable bundle while a newer map revision invalidated the store; a missing bundle was also liable to be classified as planner failure during recertification. | Fix ngay | Added identity/goal/pointer revalidation under the execution-store lock immediately around publication; missing bundle is now a pending recertification gap. `navigation_execution` 2/2 and `navigation_runtime` 7/7 passed, including deterministic invalidation tests. End-to-end executor/SITL race evidence remains open. |
| 2026-08-27, unconditional world-revision invalidation reduced liveness | Every newer map revision cleared the committed command, including revisions outside the remaining trajectory. This created repeated recertification gaps and could reduce observed duration over simulation time without proving that the trajectory was unsafe. | Fix ngay / safety contract | Added exact-bundle validation against the new immutable snapshot, generation/pointer matching, and one gated world-publication plus certificate-transfer operation. A validated bundle is copied with the new world identity; any race or failed validation clears it fail-closed. Execution 2/2 and runtime 7/7 passed. Full race distribution, dataset and repeated SITL evidence remain open. |
| 2026-08-27, CIRI numerical and diagnostic budget gaps | Tangent-plane construction and ellipsoid decomposition had unchecked degenerate/non-finite divisions, and corridor diagnostics could grow with every solve. Expensive corridor loops also did not share the planner's steady deadline. | Fix ngay / validation mở | Added finite/degenerate guards, shared deadline propagation through CIRI/corridor generation, and a bounded diagnostic point buffer. Invalid-configuration regression plus planning backend CTest 5/5 passed. Long-running deadline, memory, dataset and repeated SITL evidence remain open. |
| 2026-08-27, A* reconstructed a centered search window and ordered mutable frontier pointers | The pinned world snapshot exposes signed per-layer bounds, but A* rebuilt its own window around the current start. This could address cells outside the actual layer, while a frontier priority comparator read a node's mutable score after enqueue. A deadline after open-set exhaustion was also reported as `NO_PATH`. | Fix ngay / validation mở | A* now uses exact evidence/inflated bounds, captures frontier score and sequence in immutable queue entries, discards stale entries, and returns `TIME_OUT` when the steady deadline has expired. Planning backend CTest passed 5/5; runtime CTest passed 7/7 for ten repetitions. Sparse RSS/latency distributions, dataset and repeated SITL evidence remain open. |
| 2026-08-27, generated runtime config disabled raycasting after profile load | The canonical planner profile enabled sensor-origin raycasting for BACKUP known-free evidence, but `_mapping_params()` unconditionally rewrote the generated session planner YAML to `false`. This made source-level config and actual runtime behavior disagree. | Fix ngay / validation mở | Removed the hidden override and added contract assertions for both blocked and allow-unknown mission materializations. Runtime now preserves the canonical evidence setting; callback/RSS distributions and repeated SITL remain open because raycasting may add mapping cost. |
| 2026-08-27, mapping loader read `raycasting/p_free` twice | The same probability key was loaded twice with the same default, creating two apparent ownership points and making a future conflicting edit easy to miss. | Fix ngay / validation mở | Removed the duplicate load; no selected value or mapping behavior changed. The mapping vendor build/tests remain the verification gate. |
| 2026-08-27, propagated odometry enable switch duplicated a mandatory contract | `propagated_odometry.enabled` could disable the stream even though navigation consumes propagated odometry and the health contract required it. The switch also changed TF ownership and made diagnostics claim OK without propagation evidence. | Fix ngay / validation mở | Removed the switch and all disabled branches; propagated odometry is now always constructed, started, published, and health-gated. Rate/capacity/history/correction-age parameters remain explicit. FAST-LIO package tests, dataset, and SITL evidence remain required. |
| 2026-08-27, backup failures collapsed distinct causes | `classifySolveFailure()` and direct backup branches mapped optimizer/init/no-path returns to `PLANNER_BACKUP_FAILED`, obscuring whether the budget or the backup algorithm was the causal boundary. | Fix ngay / validation mở | Added explicit planner results for backup timeout, optimizer failure, initialization failure and no-path, with cancellation/deadline precedence. No candidate gate or safety fallback was relaxed; dataset and SITL evidence remain required. |
| 2026-08-27, workspace and package runtime profiles drifted | `config/runtime/mapping.yaml` and the package launch default were two hand-edited files, creating two apparent configuration owners. | Fix ngay / validation mở | Keep `config/runtime/mapping.yaml` as the sole source; CMake copies it into the package install tree as `navigation_runtime.yaml`. The runtime contract test guards the single-source rule. No runtime gate or planner behavior changed; launch and SITL evidence remain required. |
| 2026-08-27, runtime solve watchdog name hid its ownership | `planner_solve_timeout_s` looked like the planner's internal solve budget even though it is a runtime watchdog measured from the active solve. | Fix ngay / validation mở | Renamed it to `planner_watchdog_timeout_s` in both runtime profiles and the node; backend `solve_deadline_s` remains the planner-owned budget. Value and timeout behavior are unchanged. |
| 2026-08-27, planner unknown-space boolean duplicated mission policy | `frontend_in_known_free` was loaded independently and then overwritten from mission, while planner visualization also had a dead duplicate separate from A*. This left multiple apparent owners for one safety decision. | Fix ngay / validation mở | Replaced the planner boolean with a typed policy set from the mission contract, removed the dead planner visualization key, and retained A* visualization under its search owner. MAIN/BACKUP semantics and all fail-closed gates are unchanged; build, dataset, and SITL propagation remain required. |
| 2026-08-27, visibility horizon had overloaded fields and a negative sentinel | `safe_corridor_line_max_length` was both a configured cap and a derived effective horizon, while `sensing_horizon: -1` encoded a disabled distance without an explicit unit. | Fix ngay / validation mở | Split cap/floor/effective ownership, renamed them with metre units, and made `sensing_horizon_m: 0.0` the explicit disabled value. Existing safety numbers and gates are unchanged; fresh planner/runtime tests and dataset/SITL evidence remain required. |
| 2026-08-27, executable trajectory certificate only checked sampled chords | Polynomial curvature could bow between sampled points while endpoint/chord checks remained clear. | Fix ngay / validation mở | Added coefficient-derived acceleration bounds, adaptive curve-to-chord subdivision, and conservative inflated-cell tube checks with a bounded cell budget. Added an adversarial curved-voxel regression; full runtime/dataset/SITL latency and feasibility characterization remain required. |
| 2026-08-27, SITL preflight blocked by sandbox UDP permission | `external-mode-check` failed before launching PX4/Gazebo because the runner's XRCE UDP availability probe raised `PermissionError: [Errno 1] Operation not permitted`. No SITL artifact, planner cycle, or safety result was produced. | Môi trường | Did not remove the probe or relax the transport gate. Verify in an environment that permits the isolated XRCE UDP socket, then repeat headless SITL with exact build provenance. |
| 2026-08-27, runner regenerated the removed planner unknown-space key | The planner backend had moved to mission-owned typed policy, but runtime materialization still emitted the legacy `frontend_in_known_free` key into every generated planner file. | Fix ngay | Removed the ghost emission and updated blocked/allow-unknown profile tests; generated planner files now carry only the mission-file identity. MAIN/BACKUP policy remains fail-closed and is resolved inside the backend. |
| 2026-08-27, SITL external bridge omitted its simulation clock domain | The SITL launcher enables `use_sim_time`, but the external odometry bridge profile relied on its `ros_time` default. The bridge therefore had no valid timestamp mapping mode at startup. | Fix ngay / validation mở | Added explicit `timing.clock_domain: simulation_time` and a profile regression assertion. No timestamp, freshness, continuity, or PX4 gate was relaxed; full SITL startup and repeated scenario evidence remain required. |
| 2026-08-27, CIRI accepted a zero-length seed | A degenerate corridor seed could reach ellipsoid/orientation construction even though no stable seed axis existed; the prior tangent fix did not reject this earlier boundary. | Fix ngay / validation mở | CIRI now returns `INIT_ERROR` before geometric construction for non-finite or numerically zero-length seed segments. Added a direct regression; planning CTest and end-to-end corridor evidence remain required. |
| 2026-08-27, ignored dual-planning switch | `DUAL_PLANNING`/`--dual-planning` was threaded through the runner and artifacts but discarded by `_mapping_params`, so it suggested a behavior mode that did not exist. | Fix ngay / clarity | Removed the flag, Makefile plumbing, artifact field, stale docs and redundant test paths. The product always owns the MAIN plus BACKUP contract; no safety predicate or threshold changed. |
| 2026-08-27, allow-unknown main-only candidate rejected before certification | `authorizeAndStage()` rejected every candidate without a BACKUP suffix under `allow_unknown`, including `FINISH`/`NO_NEED` candidates whose complete remaining trajectory was already known-free. This made a valid completion path fail for the wrong reason. | Fix ngay / validation mở | Candidate authorization now uses the mission policy only when a certified BACKUP suffix exists; a main-only candidate is validated with `KNOWN_FREE` for its complete swept trajectory. Added a policy-selection regression and planning CTest 5/5; dataset and repeated SITL evidence remain required. |
| 2026-08-27, swept certificate treated the full axis-aligned box as trajectory | The first continuous tube check classified every cell in the chord bounding box, so an occupied cell near a diagonal box corner could reject a path that never entered its voxel. | Fix ngay / validation mở | Added a distance-to-chord filter within the conservative curve-plus-voxel tube and an off-tube occupied-cell regression. The safety coverage of intersecting cells remains fail-closed; planning CTest 5/5 passed and full runtime distributions remain open. |
| 2026-08-27, unused public planner interface duplicated the facade boundary | `navigation_planning` installed a `LocalPlanner` virtual interface with no production or test consumer, while the runtime used the concrete product facade. This made the package advertise two planner entry concepts without a shared implementation contract. | Fix ngay / architecture | Removed the dead header and kept value contracts in `navigation_planning`; the installed concrete boundary remains `navigation_planning_backend::PlannerFacade`. No behavior, gate or parameter changed. C++20 planning/backend build and focused CTest are required. |
| 2026-08-27, reference parity checker kept foreign implementation vocabulary in tooling | The standalone parity script encoded the historical reference tree and symbols, so product tooling still carried an alternate implementation identity after the port boundary had stabilized. | Fix ngay / architecture | Removed the unused checker from product tooling and kept provenance only in audit documentation/license records. No runtime source, planner behavior, gate or parameter changed; source-name audit and Python contract tests are required. |
| 2026-08-27, CIRI repeated the same endpoint correction | The numerical zero-radius branch tested endpoint `b` twice with identical projection and normalization code, adding redundant work and making the intended endpoint contract unclear. | Fix ngay / numerical hygiene | Removed the duplicate block; endpoint `a` and endpoint `b` are each corrected at most once, with the same fail-closed finite checks. Planner backend CTest is required; no gate or parameter changed. |
| 2026-08-27, PX4 admitted untagged state before typed epoch health | The PX4 odometry barrier was called with `ready=true`, and command admission treated unseen health as an epoch match. This allowed an untagged standard odometry stream to become implicitly authoritative before the typed estimator handshake. | Fix ngay / P0 safety | Require typed healthy health plus matching epoch for command caching and pass the real health-readiness state to the source-time barrier. No freshness value or fallback was relaxed. PX4 CTest is required; reset/clock-rollback replay remains open because standard `nav_msgs/Odometry` has no epoch field. |
| 2026-08-27, revalidation weakened main-only certificate policy | Initial authorization required `KNOWN_FREE` for a main-only candidate, but facade revalidation passed the mission `allow_unknown` policy directly. A retained main-only bundle could therefore be re-certified through UNKNOWN after a world update. | Fix ngay / P0 | Revalidation now calls the same role-aware `candidateCertificatePolicy()` as initial authorization. Main-only remains known-free; only MAIN with a complete BACKUP suffix may use mission policy, and BACKUP remains known-free. Adversarial review found the path; focused facade/runtime revalidation coverage and repeated end-to-end evidence remain open. |
| 2026-08-27, mission planning limits had a second C++ parser | PX4 External Mode validated the complete mission in one loader while runtime separately parsed only `mission.planning`, allowing schema, frame and UNKNOWN-policy drift between planner construction and mission control. | Fix ngay / architecture | Added product-owned `navigation_mission::loadMission()` and made both C++ consumers use it; runtime now validates the configured planning frame before planner construction. Shared contract, PX4 3/3 and runtime 7/7 tests pass. Python remains tooling-only and must stay outside flight authority. |
| 2026-08-27, route-reference vertical envelope was an extra hard gate | Nominal optimization rejected trajectories that left the sampled min/max Z envelope of the guide, even when the trajectory remained inside the collision-checked corridor and passed the independent dynamic/flatness checks. The fixed sample count and tolerance had no continuous-certificate basis. | Fix ngay / safety-contract clarity | Removed `vertical_guide_tolerance_m` and its rejection path. Route reference now shapes nominal objective only; corridor/world swept validation remains authoritative. Planner configuration and trajectory tests pass; repeated dataset/SITL evidence is still required to characterize feasibility and altitude behavior. |
| 2026-08-27, reset exported an empty world before certificate finalization | A localization-epoch change exported and published an empty reset snapshot, then exported the first updated map again. The first publication bypassed the common dependent-command finalization gate and added a full-map copy on reset. | Fix ngay / safety + performance | Removed the unused intermediate snapshot. The mutable map still resets and increments generation, while the first accepted post-reset observation is exported once and published through `publishAndFinalize`; no safety predicate or threshold was relaxed. Mapping/runtime build and CTest remain the verification gate. |
| 2026-08-27, physical boundary limits used negative missing-value sentinels | `traj_opt/boundary/max_*` used `-1` as a missing-value default even though the planner later rejected non-positive limits. This overloaded a physical parameter with control metadata and made the failure path harder to interpret. | Fix ngay / clarity | Loader defaults are now `0`; planner validation still rejects missing/non-positive limits, while valid YAML values and all safety limits are unchanged. Planner CTest and runtime contract verification remain required. |
| 2026-08-27, corridor certificate sampled only fixed optimizer points | The hard plane gate inspected `integral_reso` samples, so a polynomial trajectory could bow outside a corridor between samples while endpoints remained valid. | Fix ngay / safety invariant | Replaced the sampled gate with a continuous per-plane polynomial-extrema check using derivative roots, retained fail-closed handling for malformed/non-finite inputs, and added an interior-extremum regression. The configured tolerance is unchanged; world swept evidence and numerical-conditioning distributions remain open. |
| 2026-08-27, invalid map/planner geometry was accepted until a late query | Planning horizon, receding distance, corridor geometry and map extent were not validated together; malformed map probabilities and negative map-slide defaults could fail later or silently alter behavior. | Fix ngay | Shared YAML document, explicit unit-bearing planner names, fail-closed planner/world-geometry checks, strict mapping parameter validation and positive test fixtures. Remaining physical calibration still needs dataset/SITL/hardware evidence. |
| 2026-08-27, mapping default contradicted its validator | `raycasting/p_miss` defaulted to `0.70` while the mapping contract requires an informative miss below `0.5`; an omitted key therefore failed only after loading the rest of the map configuration. | Fix ngay | Changed the default to the product baseline `0.35`, retained fail-closed rejection for explicit invalid values, and re-ran the vendor mapping CTest. |
| 2026-08-27, virtual-plane point and ray semantics diverged | Snapshot point classification treated virtual planes as occupied at inclusive boundaries, while evidence/inflated ray checks used open comparisons; inflated rays also referenced base-plane heights. | Fix ngay / safety invariant | Unified inclusive boundaries and layer-specific virtual-plane bounds, removed the unknown-cell masking near inflated planes, and added a two-layer boundary regression. |
| 2026-08-27, propagated odometry could be relabeled after localization reset | `nav_msgs/Odometry` has no localization epoch; a delayed packet could arrive after reset and be assigned the new active epoch by the runtime and PX4 adapter. | Fix ngay / P0 | Added `last_propagated_state_stamp` to typed estimator health and a conservative source-time barrier in runtime/PX4; rejected samples at or before the barrier and all source-time regressions. Added timestamp/contract coverage; repeated reset replay remains required. |
| 2026-08-27, unhealthy typed estimator health had a grace path | `lio_health_grace_s` allowed External Mode to continue after a typed health sample explicitly reported unhealthy, contradicting the fail-closed health contract. | Fix ngay / P0 | Removed the parameter and both grace branches. Invalid, stale, future or non-monotonic health now fails closed; only the pre-health stationary acquisition window remains, and it is not available after any typed health sample. |
| 2026-08-27, invalid mapping batch/filter counts were silently clamped | `point_filt_num` and `raycasting/batch_update_size` were changed to 1 before strict validation, hiding malformed configuration and making the reported input differ from the applied input. | Fix ngay / clarity | Removed both clamps; invalid non-positive values now reach the fail-closed validator. Vendor mapping CTest covers the rejection path. |
| 2026-08-27, CIRI exhaustion warning checked the wrong counter value | `while (max_iter--)` ends with `-1` when all iterations are consumed, so `max_iter == 0` missed the exhaustion case. | Fix ngay / observability | Changed both checks to test `< 0`; no geometry or iteration limit changed. Planner build/test remains the verification gate. |
| 2026-08-27, safety-stop recovery grace was an unowned behavior parameter | `safety_stop_replan_grace_s` kept External Mode in charge after a settled stop without a state/certificate contract and could delay the fail-closed handover. | Fix ngay / P1 | Removed the mission field, parser key and runtime mission values. A verified replacement trajectory can still resume during the existing braking/confirmation state; otherwise the fixed safety confirmation requests PX4 Hold. |
| 2026-08-27, flatness gate slack could authorize beyond the physical envelope | `flatness_gate_margin_fraction` widened body-rate and thrust thresholds while only requiring a non-negative value; this made objective/config tuning part of the safety certificate. | Fix ngay / P1 | Removed the parameter and YAML key. Body-rate and thrust gates now use the exact configured envelope for both nominal and backup trajectories; no limit was raised. |
| 2026-08-27, absolute map revision rejected unaffected candidates | A newer map revision unconditionally invalidated a candidate even when the changed map region was outside the candidate's swept certificate. This could create unnecessary recertification gaps and reduce observed duration over simulation time. | Fix ngay / safety contract | Added conservative swept-region provenance and persistent per-revision map-change history. A stale certificate is retained only for proven disjoint changes; missing history, map sliding and intersecting changes remain fail-closed. Focused world-store, mapping, trajectory and facade tests pass after rebuilding the vendor ABI. Dataset/race/SITL evidence remains open. |
| 2026-08-27, nominal and backup loaded parameters that did not share ownership | `piece_num` and `uniform_time_en` were read for the nominal optimizer even though nominal parameterization is derived from the guide; the old `corridor_attraction_weight` name also suggested a corridor-safety term while the consumer was a nominal waypoint-quality term. | Fix ngay / clarity | Load `piece_num` and `uniform_time_en` only for the backup profile, rename the product field/key to `waypoint_attraction_weight`, and keep the backup slot disabled without loading a nominal objective. No safety gate or numeric threshold changed; planner build and focused configuration/trajectory tests are the verification gate. |
| 2026-08-27, FAST-LIO parameter test inherited a read-only ROS log directory | Running the parameter-loader binary without a writable `ROS_LOG_DIR` caused ROS initialization to throw while opening `/home/letandat/.ros/log`; the gtest suite then reported skipped tests and exited `139`, hiding the actual loader result. | Môi trường | Re-ran after sourcing ROS/workspace overlays and setting `ROS_LOG_DIR=/tmp/uav-navigation-ros-log`: all 19/19 parameter-loader tests passed. The UDP/getifaddrs warnings are sandbox diagnostics; no product fallback or test relaxation was added. |
| 2026-08-27, focused post-cleanup verification | After the nominal/backup parameter ownership cleanup and documentation synchronization, direct binaries passed planner runtime context `4/4`, map vendor `19/19`, mission contract `3/3`, navigation planning `85/85`, runtime mapping `39/39`, mapping worker `17/17`, PX4 mission `28/28`, timestamp conversion `10/10`, typed contracts `5/5`, geometric continuity `5/5`, execution bundle/state `9/9`, FAST-LIO parameter loader `19/19`, plus Python runtime `171/171`. | Đã xác minh ở mức build/unit/contract | No assertion failure remains in the focused scope. This is not dataset/SITL/hardware acceptance; repeated performance distributions, full closed-loop SITL and Git checkpoint remain open. |
| 2026-08-27, runtime retained a duplicate ROS-time wrapper | `NavigationRuntimeNode::rosTimeFromSeconds()` only forwarded to the shared `navigation_common::secondsToRosTime()` utility, creating a second apparent timestamp owner at the command boundary. | Fix ngay / architecture | Removed the wrapper and routed both command header and world-observation conversion directly through `navigation_common`. Semantics and fail-closed invalid conversion remain unchanged; runtime build and shutdown/command-clock tests are the verification gate. |
| 2026-08-27, steady-state snapshot export remained O(N) | Every ordinary mapping update rebuilt and allocated the complete evidence and inflated grid even when the backend reported a small changed region, leaving snapshot export as the dominant callback component. | Fix ngay / performance | Added validated affected-region exports and bounded immutable snapshot patches. Initial snapshots, map slides, whole-world changes, invalid/empty patches and every ninth patch update still take the full fail-closed path. Mapping actor patch-depth/continuity test, sourced mapping CTest and build pass; repeated timing distributions and SITL remain open. |
| 2026-08-27, seconds-to-ROS conversion narrowed an oversized finite value | `secondsToRosTime()` checked finiteness but cast a very large `double` to `int64_t` before validating the ROS `int32_t` seconds range, leaving undefined behavior at a shared timestamp boundary. | Fix ngay / numerical safety | Reject values above the representable ROS seconds range before narrowing; retain negative/non-finite rejection and add maximum-boundary regression. Navigation-common build/test is required; no clock-domain or freshness threshold changed. |
| 2026-08-27, runtime inspected vendor mapping configuration directly | Runtime read `rog_map::Config` through the mapping actor only to enforce three composition invariants, coupling the ROS runtime header/build to backend configuration names. | Fix ngay / architecture | Added product-owned `navigation_mapping::MappingConfiguration`; runtime now consumes only callback ownership, raycast batch size and virtual-plane state. Mutable vendor configuration remains inside the mapping adapter; runtime build and shutdown/mapping tests are the verification gate. |
| 2026-08-27, ctest invoked without the ROS/workspace overlay | Direct `ctest` launched the ament test wrapper without the sourced ROS Python environment, so every selected test failed at `import ament_cmake_test` before executing its binary. | Môi trường | Re-ran after sourcing `/opt/ros/jazzy/setup.bash` and `install/setup.bash`; `navigation_common` 1/1, `navigation_mapping` 1/1 and `navigation_runtime` 7/7 passed. Future evidence commands must source both overlays; this was not a product assertion failure and no test was relaxed. |
| 2026-08-27, mapping boundary exposed concrete storage to runtime | Runtime read snapshot byte methods and static live counters directly from `MappingWorldSnapshot`, while the full grid representation was installed as a public header. | Fix ngay / architecture | Added `MappingSnapshotMetrics` and opaque `WorldModelViewPtr` publication, removed runtime concrete calls, moved grid/storage definitions behind the mapping implementation boundary, and restricted installed headers. Sourced mapping 3/3 and runtime 6/6 passed; vendor symbol scan is clean. |
| 2026-08-27, voxel-corner DDA was direction-dependent | The legacy single-axis tie break could omit a side cell when a segment crossed an edge/corner, so A->B and B->A could inspect different occupancy evidence. | Fix ngay / safety invariant | Product snapshot traversal now enumerates every tied-axis subset and checks the endpoint explicitly. A diagonal occupied-side regression rejects in both directions; mapping 3/3, planning backend 5/5 and runtime 6/6 pass. Broad replay/performance evidence remains open. |
| 2026-08-27, generated install and CTest state outlived the source graph | Incremental install retained dangling old header links and old mapping archives/targets; prior CTest `Testing` directories also contained historical runs, making the install tree and result history ambiguous. | Fix ngay / hygiene | Reconfigured the packages, verified the current graph, and scheduled exact-path cleanup of stale generated links, archives, targets and old result directories. No source or user-owned file is removed; fresh sourced CTest is the only current evidence. |
| 2026-08-27, map-change provenance envelope stopped at raw metric bounds | Backend update bounds describe measured points, while discrete evidence/inflated cells can extend across half-voxel shells and inflation layers. A raw AABB could therefore exempt a stale candidate whose touched cell changed at a quantization boundary. | Fix ngay / P0 | Expanded each valid region by the larger inflation step plus both layer half-voxel widths and moved bounds outward with `nextafter`; invalid math becomes whole-world. Mapping 3/3, planning 5/5 and runtime 6/6 pass; replay distributions remain open. |
| 2026-08-27, role-boundary tolerance could inherit MAIN policy | A role lookup with a boundary grace/default MAIN could classify a gap or MAIN/BACKUP transition as exploratory UNKNOWN during revalidation. | Fix ngay / P0 | Require an exact complete half-open role partition and return no role for gaps; BACKUP remains known-free. Planning 5/5 and runtime 6/6 pass; producer-level floating-point and SITL evidence remain open. |
| 2026-08-27, symmetric DDA tolerance included an infinite inactive-axis sentinel | Scaling epsilon by `DBL_MAX` made tie detection numerically huge when one axis had no transition, undermining the intended symmetric supercover check. | Fix ngay / P1 | Scale ties only from finite boundary values while preserving all tied-axis subset checks; diagonal regression rejects both directions. Mapping 3/3 passes; randomized numerical/performance evidence remains open. |
| 2026-08-27, product metadata still exposed a backend release/key vocabulary | Package text contained a release version and runtime diagnostics exposed a backend configuration key even after the runtime API had become product-owned. | Fix ngay / clarity | Removed the release tag from package text, replaced the diagnostic with a product-level instruction and removed the unused runtime vendor test dependency. Private adapter/config and provenance tooling remain intentionally isolated pending typed mapping configuration. |
| 2026-08-27, default planner objects contained indeterminate scalar fields | Flatness, deadline and planner envelope fields were declared without in-class initialization; a future default-constructed helper could read undefined values before a file loader replaced them. | Fix ngay / numerical hygiene | Added explicit neutral initialization to planner and trajectory configuration types; loaded configurations retain their existing defaults and validation. Planning CTest 5/5 passes; sanitizer coverage remains open. |
| 2026-08-27, backup visibility fast path omitted the exact terminal point | Visibility sampling used only `t < total_dur`, so the all-visible branch could classify the remaining trajectory as `FINISH` without checking its true endpoint. | Fix ngay / P0 | Always append and certify the finite position at `total_dur`; no new gate or parameter was added. Planner CTest and closed-loop visibility evidence remain required. |
| 2026-08-27, bounded history rollover selected the wrong ancestor | The first retention helper walked from the previous head toward an older prefix when the chain reached its limit. New records therefore discarded recent provenance despite the depth counter remaining bounded. | Fix ngay / safety provenance | Link a saturated new record to the previous head's immediate predecessor, retaining the newest bounded window; missing older links continue to fail closed. Mapping rebuild and sourced mapping CTest are required; long-run rollover replay remains open. |
| 2026-08-27, mapping world-model test omitted the language standard | `test_mapping_world_model` had no explicit C++20 compile feature while the library and sibling mapping tests did, leaving the regression test dependent on the toolchain default. | Fix ngay / build contract | Added `target_compile_features(test_mapping_world_model PRIVATE cxx_std_20)` and retained the same test scope. Fresh mapping build and sourced CTest are required. |
| 2026-08-27, planner state was read while odometry could replace it | A solve read `robot_state_` repeatedly while `setState()` wrote the same object under a mutex, creating a data race and allowing one trajectory to mix state epochs. | Fix ngay / safety invariant | Copy one ingress state under lock at solve start and use the stable per-solve copy throughout planning. The ingress lock is not held during optimization; delayed-state and SITL evidence remain open. |
| 2026-08-27, planner-history ACK had no second world check | Execution committed the candidate before the planner-history ACK, leaving a small interval in which a world update could make the planner's private history certificate stale. | Fix ngay / safety transaction | ACK now rechecks the exact validated world under the authorizer before mutating planner history, without reversing the lock order. Execution-store identity checks remain authoritative for exposure. |
| 2026-08-27, finite quaternion norm could overflow | External finite quaternion coefficients were tested with a squared norm; large coefficients could overflow and pass a non-finite/invalid normalization path. | Fix ngay / numerical safety | Use a finite max-coefficient scale, normalize only after scaling to a bounded magnitude, and apply the rule at mapping, runtime odometry and planner state boundaries. |
| 2026-08-27, snapshot inflation radius was not validated | `occupied_inflation_radius_m` was omitted from immutable grid metadata validation, so NaN could defeat a later clearance comparison. | Fix ngay / P0 | Require a finite nonnegative radius before snapshot construction; planner geometry validation still requires it to cover the derived body envelope. |
| 2026-08-27, candidate export widened caller freshness | Candidate export used `max(valid_until, trajectory_end)`, allowing a producer to outlive the caller's hard validity deadline. | Fix ngay / P0 | Use the minimum of caller expiry and trajectory endpoint and reject an empty validity interval. |
| 2026-08-27, point-cloud decoder discarded measured intensity | PointCloud2 conversion always wrote zero intensity, making the backend intensity threshold unrelated to the sensor data. | Fix ngay / input contract | Preserve a valid `intensity` channel when present; retain zero only as the documented neutral value for schemas without that optional channel. |
| 2026-08-27, planner-history ACK left a private candidate after execution rollback | Execution invalidation removed the visible bundle but did not clear the planner's separately staged candidate when the history ACK failed. | Fix ngay / P0 | Discard the staged candidate before invalidating execution state; no new parameter or bypass. Runtime CTest 6/6 passes; injected ACK-failure coverage remains open. |
| 2026-08-27, mapping snapshot repeated byte-to-cell conversion | Full-grid export produced byte codes, then the adapter allocated and converted a second full array before immutable snapshot construction, adding avoidable callback work. | Fix ngay / P1 performance | Keep validated byte storage private to the snapshot and decode only at query boundaries; no map/certificate semantics or gate changed. Mapping CTest 3/3 passes; repeated callback/observed-duration distributions remain open. |
| 2026-08-27, provenance rollover skipped newest revision | The bounded singly linked history could not truncate its tail in constant time; once saturated, linking to an ancestor dropped the immediately previous revision and could make stale-map recertification reason over a non-contiguous interval. | Fix ngay / P0 safety | Replaced the chain with a bounded immutable newest-first record window. The consumer requires contiguous revision coverage and fails closed when the requested interval is older than retention; no safety predicate is weakened. Mapping regression and CTest are required; long-run rollover remains open. |
| 2026-08-27, world evidence freshness was not an executable gate | Runtime checked propagated-state freshness but could continue publishing or committing against an old immutable map snapshot. | Fix ngay / P0 safety | Added the existing ROS source-age check at planning, candidate-admission and command-publication boundaries. Missing/stale world evidence now cancels solving, clears exposure and rejects candidates; no new parameter or relaxed gate. Interleaving and repeated SITL evidence remain open. |
| 2026-08-27, mapping callback timing under-reported dependent work | `mapping_callback_total_us` was populated from backend map-update duration even though snapshot finalization and dependent certificate handling ran in the same callback. | Fix ngay / P1 observability | Measure the full `MappingActor::process` envelope and keep backend update/export metrics separate. This enables an honest observed-duration diagnosis without changing behavior; repeated performance distributions remain open. |
| 2026-08-27, unused waypoint optimizer remained in the product build | `GcopterWayptS3/S4` had no production include or call site but its source was still compiled, retaining an unowned optimizer surface. | Fix ngay / architecture/performance | Removed the unused source and private header from the backend target. The active `Gcopter` transform utility remains; no planner behavior or safety gate changes. |
| 2026-08-27, runtime admitted cloud and corrected odometry through a second pairing path | The runtime subscribed separately to `/lio/registered_points` and `/lio/odometry_corrected`, then attempted timestamp pairing until the first typed observation arrived. Callback order could change the active input path and expose duplicate decoding, queue skew and ambiguous epoch ownership. | Fix ngay / architecture and safety | Removed the legacy subscriptions, pairing helper, pending/history state and related parameter keys. Runtime now admits only the atomic `RegisteredScan` containing cloud, corrected pose, epoch and sequence. The shutdown test publishes the typed contract; sourced runtime CTest and Python contract tests are required. |
| 2026-08-27, startup mapping snapshot was exported twice | Startup exported the complete map once to obtain nearest offsets and again to construct the immutable product snapshot, adding a full-grid copy before the first observation. | Fix ngay / performance | Reuse nearest offsets from the single export that constructs the initial snapshot. Steady-state update export remains O(N) and is tracked separately; no safety gate or map semantics changed. |
| 2026-08-27, runtime freshness parameter name was too broad | `input_max_age_s` covered world evidence, propagated execution state and command validity, while its name implied only input arrival. Splitting it immediately would add unmeasured behavior knobs. | Fix ngay / clarity | Renamed it to `data_freshness_window_s` and documented the single runtime ownership. PX4 and bridge age parameters remain separate because their clocks and observation points differ; no value or predicate changed. |
| 2026-08-27, product profile retained a disabled legacy ROS callback | The product planner YAML still advertised raw cloud/odometry callback topics even after runtime admission became the atomic `RegisteredScan` contract. | Fix ngay / clarity | Removed the disabled `rog_map/ros_callback` block from the product profile. Vendor parser fixtures remain isolated test inputs; no runtime subscription or safety predicate changed. |
| 2026-08-27, product mapping profile exposed disabled capability knobs | ESDF, visualization, frontier extraction, load-PCD and unknown-inflation keys were present with disabled values although the product runtime had no corresponding consumer contract. | Fix ngay / clarity | Removed the disabled blocks from `src/runtime/navigation_runtime/config/planner.yaml`; vendor defaults and compatibility fields remain private to the adapter. Mapping behavior, raycasting and virtual-plane settings are unchanged; mapping/runtime tests are required. |
| 2026-08-27, backup object was mistaken for an executable suffix | Candidate construction marked `backup_suffix_available` from a non-null backup object, even if the resulting final BACKUP interval had zero duration. Runtime could then treat a malformed candidate as having a usable safety suffix. | Fix ngay / P0 safety | Derive the flag from the finite positive-duration final BACKUP interval and reject a non-null backup without that interval. Added a zero-duration builder regression; planner/runtime tests required. No UNKNOWN or geometry gate was relaxed. |
| 2026-08-27, final sim preflight blocked by sandbox networking | `runner.py sim-check` stopped before launching PX4/Gazebo because the environment rejected the UDP socket probe with `PermissionError: [Errno 1] Operation not permitted`; no runtime session was created. | Môi trường | Recorded as infrastructure evidence, removed the two stale runtime lock files after confirming no matching process/session, and did not relax the port check. SITL remains unverified until a runner with UDP permission is available. |
| 2026-08-27, propagated-state observers kept the obsolete odometry type | The product topic had migrated to `navigation_contracts/msg/PropagatedOdometry`, but the dataset shadow planner and monitor still subscribed as `nav_msgs/Odometry`; replay and mapping received data while planner/report propagated-state evidence stayed empty. | Fix ngay / validation integration | Both observers now use the typed envelope, require valid producer identity for shadow goals, and decode the nested odometry for stream accounting. Runtime Python contract coverage was updated; a fresh bounded dataset run is required before closing the validation item. |
| 2026-08-27, dataset shadow planner kept the obsolete propagated odometry type | The product topic had migrated to `navigation_contracts/msg/PropagatedOdometry`, but the validation helper still subscribed as `nav_msgs/Odometry`; it therefore timed out before publishing a synthetic goal even though replay and mapping were receiving data. | Fix ngay / validation integration | Subscribe with the typed envelope, require positive epoch/sequence, and pass its nested odometry to the existing geometry logic. Runtime Python contract coverage was updated; a fresh bounded dataset run is required before closing the validation item. |
| 2026-08-27, dataset replay omitted the mission policy boundary | After the typed propagated-state fix, replay published the synthetic goal but the planner fell back to `RequireKnownFree` because dataset launch passed no `mission_file`; A* timed out before reaching the optimizer. | Fix ngay / validation integration | Added one explicit `recorded_replay` mission contract and pass its resolved file to planner runtime. MAIN `allow_unknown` is benchmark-scoped; BACKUP remains independently `require_known_free`, and no hardware/runtime gate is relaxed. Fresh build and complete replay are required to verify the next planner stage. |

## Điều kiện đóng nhật ký

Nhật ký chỉ được đóng khi toàn bộ mục “ghi nhận sau refactor” đã có bằng chứng
đóng, các bypass tạm thời đã được gỡ hoặc có quyết định an toàn rõ ràng trong
decision ledger, và validation cuối đã truy vết ngược được mọi lỗi về commit,
test, scenario và artifact. Không dùng một lần chạy đơn lẻ để nâng ngưỡng hoặc
tuyên bố acceptance SITL/hardware.
