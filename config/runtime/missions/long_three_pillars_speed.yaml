mission:
  version: 1
  id: external_mode_long_three_pillars_speed
  frame: lio_odom
  waypoints:
    - id: speed_start
      position: [0.0, 0.0, 3.0]
      acceptance_radius_m: 0.9
      behavior: pass_through
    - id: speed_finish
      position: [140.0, 0.0, 3.0]
      acceptance_radius_m: 0.9
      behavior: stop
      hold_s: 0.5
  planning:
    max_velocity_mps: 5.0
    # Repeated same-profile traces showed that the former 2/4 envelope
    # rejected otherwise bounded hot-replan candidates while the vehicle was
    # already entering its certified braking suffix. Keep this well below the
    # 12/30 product envelope and the requested non-high-dynamic regime.
    max_acceleration_mps2: 3.0
    max_jerk_mps3: 8.0
    unknown_policy: allow_unknown
  control:
    acceptance_speed_mps: 0.15
    acceptance_confirmation_s: 0.5
